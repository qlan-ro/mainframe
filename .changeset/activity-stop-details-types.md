---
'@qlan-ro/mainframe-types': minor
'@qlan-ro/mainframe-ui': minor
---

Activity rows gain a capability-gated stop control, a drill-in detail view with an on-demand output tail, and terminal-row retention (completed/failed/stopped rows stay listed with their summary and usage until dismissed or the next turn, instead of vanishing). Monitor-tool work now renders its own kind, and a task with an unrecognized type shows its reported type instead of a generic label.
