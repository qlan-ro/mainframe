---
"@qlan-ro/mainframe-app-tauri": patch
---

Set the Tauri app icon to the Mainframe icon (regenerated from the Electron
app's `resources/icon.png`), replacing the default Tauri placeholder logo across
the desktop icon set (icns/ico/PNG sizes + Windows Square/Store logos).
