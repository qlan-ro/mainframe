---
"@qlan-ro/mainframe-app-tauri": minor
---

Add the three-axis appearance system (Mode × Colour Scheme × Window Style) and the
2026-06-12 chrome pass: dark-classic redesign, ocean + velvet schemes, per-mode×scheme
accents, unified/split/glass window styles, the status bar retired into a sidebar
footer (Connected + derived session counts), the surface rail moved into the toolbar,
and the session-row unread-answer system. Switching is store-only (no Settings UI yet);
the Update pill is deferred pending a Tauri updater.
