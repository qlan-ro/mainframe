---
'@qlan-ro/mainframe-app-tauri': minor
---

Add the bottom Context / Skills / Agents tabbed panel to the left sidebar (below
the session list), with a 5px resize handle and persisted tab + height. Context
re-fetches on the `context.updated` event; image attachments open the existing
lightbox. Completes sidebar parity with the warm-chrome artboard.
