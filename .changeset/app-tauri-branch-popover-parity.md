---
"@qlan-ro/mainframe-app-tauri": patch
---

Branch popover parity vs prototype `13-popover.jsx` (the `popovers` surface from the
artboard audit, not covered by the Phase-3 batch): search field now `h-[30px]` with a
0.5px hairline + `rounded-md` + 13px icon; the Fetch button uses the refresh glyph
(`RefreshCw`, was `ArrowDownLeft`) on a `bg-background` 0.5px-hairline square; "Update
All" → "Update all" with the `⤓` hint; Push uses `ArrowUp`; the local section header
reads "Local branches" (→ LOCAL BRANCHES) instead of "Local".
