---
"@qlan-ro/mainframe-app-tauri": minor
---

Rebuild the ⌘O command palette as a four-mode spotlight: files+sessions, `>` commands, `@` symbols (real LSP workspace/symbol), and `#` changed files. Drops cmdk for a custom mode engine, fixes the stale shouldFilter double-filter, and matches the warm-chrome artboard.
