---
'@qlan-ro/mainframe-app-tauri': minor
---

Add an in-chat Find bar (Cmd/Ctrl+F) — highlight and navigate query matches in the
chat transcript via the CSS Custom Highlight API, with prev/next + count. Scoped to
chat: the CodeMirror editor keeps its own Cmd+F search. v1 searches message text only.
