---
"@qlan-ro/mainframe-app-tauri": patch
---

Open markdown files in Preview (rendered) by default instead of the source
editor, matching the other special viewers (SVG/CSV/image/PDF, which already
render on open). Switch to Source from the header toggle to edit.
