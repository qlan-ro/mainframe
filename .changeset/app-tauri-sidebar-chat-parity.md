---
"@qlan-ro/mainframe-app-tauri": patch
---

Sidebar + chat deeper parity pass: user-message bubbles use the fixed 470px
artboard max-width (was 75% adaptive); the Sessions group header gains its leading
chevron; the AnswerPill is a fixed 16px-high 7px-padded chip; session-row hover
actions round to 4px; the code-reference card header/footer use content2 (not the
warmer raised); the tag filter bar uses a 0.5px hairline; and the Task tool-card
tightens to 10/7px padding.
