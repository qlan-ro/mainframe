---
---

Rust automations engine (`mainframe-automations` crate in `packages/core-rs`) — no user-facing package change; ships behind `MAINFRAME_DAEMON_IMPL`. Parity with the Node Automations v2 engine on the ratified contract.
