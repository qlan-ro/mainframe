---
"@qlan-ro/mainframe-app-tauri": patch
"@qlan-ro/mainframe-app-electron": patch
---

Update desktop app icons to the new Mainframe brand mark (M monogram on a warm-paper squircle). Refreshes the Tauri (`src-tauri/icons/`) and Electron (`resources/`) icon sets, adds a Windows `icon.ico` to the Electron build, and drops the stale legacy `64x64.png` and `icon.svg`.
