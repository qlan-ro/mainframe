---
'@qlan-ro/mainframe-ui': patch
---

Give composer attachment pills a fixed width instead of a content-driven cap, so every pending attachment renders the same size regardless of file name length.
