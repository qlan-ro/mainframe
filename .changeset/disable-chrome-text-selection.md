---
"@qlan-ro/mainframe-app-tauri": patch
---

Disable text selection on the app-shell chrome (sidebar, headers, labels, pills,
buttons) to match the desktop app — selection is re-enabled only on real content
(rendered markdown / chat messages, form fields, and code blocks).
