---
"@qlan-ro/mainframe-ui": patch
---

Editor inline review comments can now be sent to the active chat — per-comment Send or batch "Submit review" — and render as review-comment cards. Comments capture the full selection (or hovered line), and the inline card matches the preview-annotation styling.
