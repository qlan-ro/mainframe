---
'@qlan-ro/mainframe-core': patch
'@qlan-ro/mainframe-desktop': patch
---

Session list now re-orders correctly when a chat gets new activity, and switching sessions while another is being archived no longer blocks the UI.
