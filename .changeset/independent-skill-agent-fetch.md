---
'@qlan-ro/mainframe-ui': patch
---

The composer loads its skills and agents independently, so a failing skills fetch no longer empties the `@` agents picker and a failing agents fetch no longer empties the `/` skills picker.
