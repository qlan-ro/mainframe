---
---

app-tauri migration (no consumer changelog): thermo-nuclear review-fixes — shared card-shell + resolveResultText (collapse 5 duplicated card scaffolds), one MainframeMessageMeta metadata contract + single toJsonArgs cast site, group summaries carried in the projection, native grouped reasoning, rich SkillLoadedCard from the system message (dead _SkillLoaded arm removed), native DirectiveText @mention chips, markdown dedup (one highlight path), warm-chrome thin scrollbar, and dead-code deletions. Internal WIP behind the new Tauri UI.
