---
"@qlan-ro/mainframe-ui": patch
---

Preview: when a remote-daemon tunnel fails, show an in-body "Preview tunnel unavailable" state instead of replacing the whole preview with full-pane logs. The console stays a drawer, matching the normal running layout.
