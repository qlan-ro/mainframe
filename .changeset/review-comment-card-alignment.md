---
'@qlan-ro/mainframe-ui': patch
---

Fix a review-comment (file-reference quote) user turn rendering left-aligned instead of right-aligned like every other user message, by having the card declare its own end alignment.
