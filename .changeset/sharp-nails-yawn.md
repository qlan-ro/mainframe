---
---

app-tauri migration (no consumer changelog): design-conformance fixes vs the warm-chrome artboards — fix 2 invisible-error blockers (opaque --mf-destructive-tint replaces silent /opacity-on-hex bg-destructive/N), ghost-variant reasoning with streaming shimmer + left-indent, BashCard family tile + Run verb, StatusDot Running/Failed/Done labels, per-kind slash-pill tint, amber→red bash errors, queued border-spinner, plain diff stat text, and ~16 token/spacing polish deltas. Internal WIP behind the new Tauri UI.
