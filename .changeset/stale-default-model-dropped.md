---
'@qlan-ro/mainframe-core': patch
---

A saved provider default model that the adapter no longer offers is dropped when a new chat is created, instead of being handed to the CLI as an unknown model id.
