---
'@qlan-ro/mainframe-ui': patch
---

Order the session rail's Tasks panel by last touched, in-progress tasks above open ones, so a task you just added or edited sits at the top instead of the bottom.
