---
'@qlan-ro/mainframe-ui': patch
'@qlan-ro/mainframe-types': patch
---

Show images returned by a tool call (e.g. a screenshot from Read or an MCP tool) as clickable thumbnails on the tool card instead of a `[Image: ...]` text note or, worse, raw JSON.
