---
'@qlan-ro/mainframe-ui': patch
---

Rebase the v2 UI clone on the stock shadcn Luma preset. The custom token layer — named type rungs, compressed spacing, `mf-*` colours, three colour schemes, three window styles — is gone; v2 now renders on the preset's sheet with the macOS system blue as `--primary`.
