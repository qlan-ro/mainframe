---
'@qlan-ro/mainframe-app-tauri': minor
---

User-message states completed per the design artboard: file-attachment pills (native message.attachments), sandbox-capture context chips (sentinel parsed in the projection — previously rendered as raw text), and the render-only code-reference card (producer lands with the editor surface).
