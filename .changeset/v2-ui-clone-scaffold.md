---
---

Scaffold `packages/ui/src/v2`, a parallel UI build where the spacing and type
scales are rebuilt. Dev-only — `vite build` still takes `index.html` as its sole
input, so nothing here reaches a packaged app and no shipped surface changes.
