---
"@qlan-ro/mainframe-desktop": patch
---

Fix the first-run tutorial highlighting the wrong elements — steps 1 and 2 now point at the add-project and new-session buttons — and stop the projects/chats stores from being clobbered when the websocket reconnects.
