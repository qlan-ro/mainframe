---
"@qlan-ro/mainframe-ui": minor
"@qlan-ro/mainframe-core": minor
---

Workflow builder: per-step config panels with magic-variable chips; the visual model reaches full DSL parity and the YAML pane becomes a read-only generated preview. The daemon accepts `form:` as a canonical alias for `question:`.
