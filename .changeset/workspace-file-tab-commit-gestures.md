---
"@qlan-ro/mainframe-ui": patch
---

The workspace file tree now supports the conventional gestures for keeping a file open: double-click, accelerator-click (⌘ on macOS, Ctrl elsewhere), or middle-click a row to open it as a permanent tab instead of the single-click preview slot. The row's context menu also gains a "Keep open" item for the same purpose.
