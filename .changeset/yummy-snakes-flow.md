---
---

Agent QA tooling only — no package behavior changes. Adds a packaged-build QA variant for the tauri-mcp bridge (build-time feature + config overlay, an isolated launch script, and a recipe) so packaged-only scenarios like CSP enforcement can be driven end to end.
