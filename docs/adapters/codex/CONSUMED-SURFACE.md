# Codex CLI consumed-surface checklist

Source of truth: `packages/core-rs/crates/mainframe-adapter-codex/`. Every
notification and result shape the crate deserializes is declared as a typed
struct in `src/types.rs`/`src/item_types.rs` — unlike the Claude crate,
serde does the shape-checking here. That makes drift *quieter*, not safer: a
struct that fails to deserialize is logged and dropped by the caller
(`if let Ok(p) = serde_json::from_value(...) { ... }`), so a renamed field
degrades exactly like an unrecognized Claude event, just one layer further
from the wire. The crate ships **no fixture directory** — every test in
`tests/` builds its JSON-RPC payloads inline — so "add a regression test"
recommendations below must name a test file and the payload to pin, not just
say "add coverage".

See `docs/adapters/README.md` for the row schema, ID grammar, and citation
rules.

| ID | Surface | Upstream artifact | Mainframe consumer (file::symbol) | Coverage | Verified | Breakage symptom |
|---|---|---|---|---|---|---|
| CODEX-RPC-01 | `initialize`/`initialized` handshake | Request `{clientInfo, capabilities: {experimentalApi: true}}`, 10s handshake timeout | `src/session.rs::initialize_params` | `tests/list_models.rs::probes_models_with_the_configured_executable_path` | — | Session never reaches Ready; every subsequent RPC fails |
| CODEX-RPC-02 | `thread/start` / `thread/resume` | Params `cwd`, `approvalPolicy`, `sandbox`, `model`, and three booleans: `experimentalRawEvents`, `persistExtendedHistory`, `persistFullHistory`. Per `.claude/skills/codex-protocol-debugger/SKILL.md`, `ThreadStartParams` only documents these as optional, but `experimentalRawEvents` and `persistExtendedHistory` are **required** — omitting either fails silently or returns a cryptic error. The *response*'s `model` field (required string in `ThreadStartResponse`/`ThreadResumeResponse`, codex-cli 0.144.3) is now consumed too: it is the resolved model the app-server picked, captured as the turn-start fallback for a chat with no configured model. Deserialized leniently (`#[serde(default)]`) so an older build that omits it doesn't fail the whole response — the turn-start resolver has a further fallback tier | `src/session.rs` (the `thread/start`/`thread/resume` param-building block and `ensure_thread`), `src/session_state.rs::CodexSessionState::reported_model` | `tests/turn_start_model.rs` | 2026-08-09 (codex-cli 0.144.3, generate-ts) | New sessions fail to start, or resumed sessions lose raw-event / extended-history data with no error surfaced; a model-less chat that stops capturing the reported model loses its turn-start fallback and falls through to the provider default |
| CODEX-RPC-03 | `turn/start` / `turn/interrupt` | `turn/start` params: `threadId`, `input`, `approvalPolicy`, `sandboxPolicy`, `collaborationMode.settings.*`, optional `model`/`serviceTier`/`personality`/`summary`. `turn/interrupt` params: `{threadId, turnId}`. `collaborationMode.settings.model` is **required and non-nullable**, unlike its `reasoning_effort`/`developer_instructions` siblings (required-but-nullable) — a turn-start with the key omitted or `null` is rejected with `-32600`, so the daemon resolves a concrete id (configured model → app-server-reported model → provider/catalog default) before every turn | `src/session.rs` (turn/start param block), `src/turn_config.rs::build_turn_config`, `src/turn_model.rs::resolve_turn_model` | `src/turn_config.rs::puts_effort_in_settings_fast_as_service_tier_codex_extras_top_level`, `src/turn_config.rs::serializes_the_model_key_for_every_turn`, `tests/turn_start_model.rs` | 2026-08-09 (codex-cli 0.144.3, generate-ts) | A turn never starts, or interrupt silently no-ops (turn keeps running); an unresolvable model surfaces `-32600 Invalid request: missing field 'model'` and no turn starts |
| CODEX-RPC-04 | `thread/read` | `{threadId, includeTurns: true}`; used for temp-app-server history reconstruction and to enumerate `CollabAgentToolCall` `wait` items for child-thread ids | `src/session.rs` (thread/read call sites), `src/thread_registry.rs::lookup_agent_metadata` | `tests/history.rs::extracts_text_from_content0_text_the_thread_read_shape` | — | Resumed history comes back empty, or sub-agent child threads never get inlined |
| CODEX-RPC-05 | `model/list`, `account/rateLimits/read`, `account/read` | Model catalog probe; quota puller spawns a temp app-server solely to call these two account RPCs | `src/adapter.rs::map_codex_model`, `src/quota_pull.rs` | `tests/list_models.rs::probes_models_with_the_configured_executable_path`, `src/quota_identity.rs::returns_the_account_read_email_for_a_chatgpt_account` | — | Model picker or quota gauge goes stale/empty |
| CODEX-EVT-01 | Handled notifications | `thread/started`, `turn/started`, `item/started`, `item/completed`, `item/plan/delta`, `turn/completed`, `thread/tokenUsage/updated`, `thread/compacted`, `account/rateLimits/updated` | `src/event_mapper.rs::handle_notification` | `tests/event_mapper.rs::turn_completed_reports_context_tokens_from_prior_token_usage` | — | The corresponding UI signal (turn status, item stream, plan text, quota) stops updating |
| CODEX-EVT-02 | Known-but-ignored notifications | `turn/diff/updated`, `turn/plan/updated`, `thread/closed`, `thread/status/changed`, `item/agentMessage/delta`, `item/commandExecution/outputDelta`, `item/fileChange/outputDelta`, `item/reasoning/summaryTextDelta`, `item/reasoning/textDelta`, `thread/name/updated` | `src/event_mapper.rs::handle_notification` (the `=> {}` match arm) | none | — | N/A — this is the standing adoption-opportunity list, not a risk surface |
| CODEX-EVT-03 | `codex/event/*` blanket drop | The entire raw-event namespace Mainframe opts into via `experimentalRawEvents: true` (CODEX-RPC-02) is received and discarded unread | `src/event_mapper.rs::handle_notification` (`method.starts_with("codex/event/")`) | none | — | N/A — Mainframe pays the raw-event opt-in cost but consumes none of it; any future use is a from-scratch integration |
| CODEX-EVT-04 | `thread/tokenUsage/updated` | Notification carrying a thread's running token totals. Codex 0.144.3 sends camelCase `tokenUsage: { last, total }`; older builds still send a top-level, snake_case `usage` object. `resolved_usage()` tries `usage`, then `tokenUsage.last`, then `tokenUsage.total`; the resolved totals become `state.last_usage`, which the context gauge divides by the model's context window | `src/types.rs::{TokenUsageUpdatedParams, TokenUsageUpdatedParams::resolved_usage}`, `src/turn_lifecycle.rs::handle_token_usage` | `src/types.rs::tests::{resolved_usage_prefers_the_captures_token_usage_last, resolved_usage_falls_back_to_legacy_top_level_usage, resolved_usage_is_none_when_neither_shape_is_present}` | — | Codex sessions show no context-usage percentage: params that match neither shape are discarded, so `state.last_usage` stops updating and nothing errors |
| CODEX-EVT-05 | stdout/stderr conventions | Multiple concatenated JSON objects per line; stderr classifiers `is_rfc3339_prefix`, `is_tracing_line` (`TRACE\|DEBUG\|INFO\|WARN\|ERROR`), `is_panic_line` (`thread '…' panicked`) | `src/jsonrpc.rs::{parse_jsonrpc_messages, find_json_object_end, is_rfc3339_prefix, is_tracing_line, is_panic_line}` | `src/jsonrpc.rs::dispatches_a_valid_object_before_trailing_stdout_noise_on_the_same_line` | — | A notification silently merges with trailing log noise and fails to parse, or a real crash gets misclassified as routine log output |
| CODEX-EVT-06 | Rate-limit window normalization | Keyed by `windowDurationMins` (`300`→session, `10080`→weekly), never by `primary`/`secondary` slot; unrecognized durations dropped, not zeroed; `resetsAt` seconds→ms | `src/quota_rate_limit.rs::{kind_by_duration_mins, normalize_rate_limit_snapshot, map_window}` | `src/quota_rate_limit.rs` (window-mapping tests) | — | A new upstream window duration is silently discarded — quota gauge misses a bucket with no error |
| CODEX-ITEM-01 | `ThreadItem` union (18 variants) | `AgentMessage`, `Reasoning`, `CommandExecution`, `FileChange`, `McpToolCall`, `WebSearch`, `ImageGeneration`, `TodoList`, `UserMessage`, `CollabAgentToolCall`, `ContextCompaction`, `SubAgentActivity`, `DynamicToolCall`, `EnteredReviewMode`, `ExitedReviewMode`, `ImageView`, `Sleep`, `HookPrompt` — one row for the whole union; seven of these variants were added for Codex 0.144.3, the highest-churn mapping point in the adapter. Unknown item variants are dropped, not errored | `src/item_types.rs::ThreadItem`, payload fields in `src/thread_item_variants.rs`, `src/types.rs::deserialize_lenient_items` | `tests/item_types.rs::sub_agent_activity_tolerates_an_unknown_extra_field` | 0.144.3 (seven variants added) | A new upstream item type renders as nothing — no error, no placeholder, the item simply never reaches the transcript |
| CODEX-CTRL-01 | Server→client approval/input requests | `item/commandExecution/requestApproval` (`itemId`, `command`, `cwd`), `item/fileChange/requestApproval` (`itemId`, `reason`), `item/tool/requestUserInput` (`itemId`/`toolCallId`, `questions[].{id,question}`, `options[][].label`); reply shapes `{decision}` / `{answers}`; any other method auto-declines with `{decision: "decline"}` | `src/approval_handler.rs` (the method-dispatch block) | `tests/approval_handler.rs::routes_to_ask_user_question_when_plan_mode_false` | — | Approval/AskUserQuestion/plan-exit prompts stop reaching the user, or an unrecognized method wrongly auto-declines a real approval |
| CODEX-FLAG-01 | app-server spawn argv and env | Argv is exactly `<binary> app-server` — everything else is negotiated over JSON-RPC; env `FORCE_COLOR=0`, `NO_COLOR=1`, resolved login-shell `PATH` | `src/session.rs::build_app_server_command` | none | — | CLI never starts, or starts with the wrong PATH so packaged builds can't find dependent tools |
| CODEX-FLAG-02 | Turn-config vocabularies | `personality ∈ {none, friendly, pragmatic}`, `reasoning_summary ∈ {auto, concise, detailed, none}`, `service_tier` only ever `"fast"` (never `"flex"` — rejected by some models) | `src/turn_config.rs::{CodexProviderTuning, CodexTurnConfig, build_turn_config}` | `src/turn_config.rs::service_tier_fast_when_tuning_fast_true_and_undefined_when_false` | — | An invalid enum value is rejected by the CLI, or a stale value silently no-ops |
| CODEX-FLAG-03 | UI-side protocol enum mirror | `SUMMARY_OPTIONS`/`PERSONALITY_OPTIONS` hardcode the same vocabulary as CODEX-FLAG-02, independently of the Rust crate | `packages/ui/src/features/settings/panes/providers/CodexTuningDefaults.tsx` | none | — | Settings UI offers a value the CLI no longer accepts (or is missing a newly added one) |
| CODEX-FLAG-04 | Title-generation one-shot argv | `exec --ephemeral --ignore-user-config --skip-git-repo-check -C <tmpdir> -s read-only --color never <prompt>`, prompt last. `--ephemeral` is load-bearing: without it the run writes a rollout JSONL (CODEX-FILE-01) and a `threads` row (CODEX-FILE-02) that surface in the user's `/resume` list. `--ignore-user-config` skips the user's MCP servers, skills, and plugins — 7k tokens per title instead of 20k; auth still resolves from `CODEX_HOME`. The temp cwd keeps the project's `AGENTS.md` out of the prompt. The title is stdout in full; the banner and token accounting go to stderr | `src/title_generator.rs::{generate_codex_title, title_args}` | `src/title_generator.rs::{runs_ephemerally_and_ignores_user_config, never_writes_from_the_title_run, the_prompt_is_the_final_positional_arg}` | 0.144.3 | An unknown flag exits rc=2 with `error: unexpected argument`, so titles loudly fall back to the truncated first message; if `--ephemeral` is dropped or renamed, every title generated leaves a ghost session behind |
| CODEX-FILE-01 | Rollout JSONL session files | `~/.codex/sessions/YYYY/MM/DD/rollout-<timestamp>-<threadId>.jsonl`; `session_meta` head record; stripped preamble prefixes; `{type, payload}` envelope with six payload types; `exec_command`/`mcp__*`/`apply_patch` reconstruction for sub-agent child threads | `src/external_sessions.rs::{codex_sessions_root, list_external_sessions}`, `src/external_session_parse.rs::{parse_lines, extract_meta}`, `src/rollout_reader.rs`, `src/rollout_reconstruct.rs` | `tests/external_sessions.rs`, `tests/rollout_reader.rs::message_line` (fixture builder) | — | "Resume external session" list goes empty, or sub-agent history reload comes back without child-thread bash/file-edit/MCP steps |
| CODEX-FILE-02 | `~/.codex/state_5.sqlite` thread registry | Read-only `SELECT id, agent_nickname, agent_role, rollout_path FROM threads WHERE id IN (...)`; the `_5` suffix is an upstream schema version — a version bump is itself a breaking signal | `src/thread_registry.rs::lookup_agent_metadata` | none | — | Sub-agent TaskGroup cards lose their nickname/role title and fall back to a generic label |
| CODEX-FILE-03 | `~/.codex/auth.json` identity | `{tokens: {account_id}}` fallback after `account/read`'s email; no `config.toml` is ever read, and `codexHome` from `initialize` is never used to locate one | `src/quota_identity.rs::read_codex_account_identity` | `src/quota_identity.rs::falls_back_to_auth_json_account_id_when_the_chatgpt_account_has_no_email` | — | Quota pull misattributes usage to the wrong account, or reports the transient-identity sentinel indefinitely |
| CODEX-FILE-04 | Rollout unified-exec tool-call pair | The modern Codex shell tool writes `custom_tool_call` `name:"exec"` whose `input` is a JS snippet wrapping `tools.exec_command({"cmd":…,"workdir":…})`, paired with a `custom_tool_call_output` whose `output` is an **array** of `input_text` blocks rather than the plain string every other tool-call kind uses. `thread/read` (CODEX-RPC-04) never returns `commandExecution` items at all (probed against codex-cli 0.147.0 on four real threads), so this rollout reconstruction is the only path that recovers Codex command output for offline scanning (e.g. PR detection, todo #339) | `src/rollout_reconstruct.rs::{handle_custom_tool_call, handle_custom_tool_call_output}`, `src/rollout_reader.rs::RolloutOutput` | `tests/rollout_unified_exec.rs::{unified_exec_reconstructs_command_execution_with_the_pr_url, unified_exec_cmd_given_as_an_array_is_joined_with_spaces, unified_exec_malformed_input_is_skipped_without_panicking_and_neighbours_still_parse, apply_patch_output_stays_string_shaped_after_widening_the_output_field}` | 0.147.0 | A future exec-wrapper change silently stops command-execution reconstruction — Codex cold-load PR scanning (and anything else built on `load_scan_records`) goes quietly empty again |
| CODEX-PROBE-01 | `codex --version` parsing | Hand-rolled scan for the first `N.N.N` triple; a prefixed build number would mis-parse | `src/adapter.rs::parse_version` | `src/adapter.rs` (version-parsing unit test) | — | Version-gated behaviour picks the wrong branch, or the version badge shows nothing |
| CODEX-PROBE-02 | `model/list` field mapping and tool categories | `displayName`, `description`, `isDefault`, `supportedReasoningEfforts`, `defaultReasoningEffort`, `additionalSpeedTiers` (containing `"fast"`), `supportsPersonality`, `hidden`; hardcoded tool categories `todo_list` (hidden+progress), `CollabAgent` (subagent) | `src/adapter.rs::{map_codex_model, get_tool_categories}` | `tests/list_models.rs::probes_models_with_the_configured_executable_path` | — | New/renamed model fields degrade the model picker silently; a renamed tool falls out of its hidden/progress/subagent treatment |

## Not consumed — adoption candidates

- The ignored notification set in CODEX-EVT-02 and the entire `codex/event/*` raw-event namespace in CODEX-EVT-03 — Mainframe already pays the `experimentalRawEvents: true` opt-in cost (CODEX-RPC-02) but reads none of it.
- `thread/list` — dropped from the port (#430); external sessions are discovered by scanning rollout JSONL directly (CODEX-FILE-01) instead of asking the app-server.
- `config.toml` and the `codexHome` field returned by `initialize` — never read; account/session state comes entirely from `~/.codex/auth.json` and `~/.codex/state_5.sqlite` (CODEX-FILE-02, CODEX-FILE-03).
