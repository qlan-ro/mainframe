# assistant-ui Feature Inventory (native vs our re-implementation)

The authoritative use-native checklist — every assistant-ui primitive/UI/hook (cloned `react@0.14.14`) mapped against our desktop chat code, so we never rediscover a re-implementation piecemeal. Generated by the inventory sweep (2026-06-05). Pair with `packages/app-tauri/CLAUDE.md` (the golden rule) + the chat build order in `MIGRATION-TRACKER.md`.

**Headline:** most of our "re-impls" are the *correct* pattern — native plumbing + custom content. `ThreadPrimitive.Root/Viewport/Messages`, `ComposerPrimitive.*`, `AttachmentAdapter`, `MarkdownTextPrimitive`, `useExternalStoreRuntime` are all already native. The genuine work is: retire a handful of true hand-rolls, adopt a few net-new natives, and make ~8 design-vs-native decisions.

## Switch to native (retire our hand-roll)
- **`QuoteOnSelectionButton`** → native `SelectionToolbar` (`getSelectionMessageId` gives per-message attribution free).
- **`renderToolCard` mega-switch + double registration** → one `tools.by_name` map, `Fallback` → `DefaultToolCard`. (Cards are currently registered twice: `makeAssistantToolUI` *and* the Fallback switch — pure duplication.)
- **Bottom card sibling `<div className="shrink-0">`** → `ThreadPrimitive.ViewportFooter` (so composer/permission-card height registers as content inset — fixes tall `PermissionCard` overlapping the last message + wrong scroll target). *Real bug today.*
- **Deprecated context hooks** → `useThread()`→`useAuiState((s)=>s.thread...)`; `useMessage()` (5 sites)→`useAuiState((s)=>s.message)`; `ThreadPrimitive.Empty`→`AuiIf`; `ThreadPrimitive.Messages components` prop → children render fn.
- **Custom `SendButton`** (normal path) → `ComposerPrimitive.Send` (keep custom only for captures-only send); `AttachmentPrimitive.Name` custom span → native.
- **`SyntaxHighlightedCode`** wiring → plug the shiki engine into the native `components.SyntaxHighlighter` slot (keep the shiki engine).
- **Dead `ExternalStoreThreadListAdapter`** (wired, rendered by nothing) → delete or commit to native thread-list.

## Net-new native adoptions (we have nothing today — pure gain)
- **`ThreadPrimitive.ScrollToBottom`** (no scroll-to-bottom affordance today).
- **`ComposerPrimitive.AttachmentDropzone`** (no drag-drop today).
- **Message `ActionBarPrimitive`** (copy/export at least).

## Design-vs-native decisions (stop-and-ask)

**→ RESOLVED 2026-06-05 (see `MIGRATION-TRACKER.md` "Open decisions" + `CLAUDE.md`):** part-grouping = **go native** (via the `convert-message` projection); subagent = **native `part.messages`**; sessions = **hybrid** (global runtime + native item rows in our layout); reasoning = **native, collapsed**; quote = **native UI + CLI glue**; errors = **keep text-routing**; queue = **keep daemon-backed**. Raw findings retained below for context.

1. **Part grouping** — our **daemon-side** synthetic `_ToolGroup/_TaskGroup/_TaskProgress` (server-summarized pipeline) vs native client-side `GroupedParts` + `groupPartByType` + `display:'standalone'`. Big architectural call. *(Resolved: go native via projection.)*
2. **Subagent/Task rendering** — `TaskCard` parses a `<usage>` string from flattened parts vs native `PartPrimitive.Messages` (real readonly nested thread). Native requires the **daemon to attach `messages` to Task tool-call parts** (pipeline change).
3. **Thread-list / sessions sidebar** — native `ThreadListPrimitive` (flat, per-runtime) vs our **global cross-project sidebar** (grouping, filter pills, tags, PR/worktree badges, status dots, pin). **Domain conflict — the one place a re-impl may be justified.**
4. **Reasoning** — native shadcn `Reasoning` (shimmer/auto-open) vs our `ThinkingPart` (currently wired OFF). Product choice: show reasoning or not.
5. **Quote model** — native structured quote (`setQuote`/metadata) vs our `> ` markdown injection. **CLI wire format has no quote concept**, so adapter glue (serialize quote→text on `onNew` + round-trip `metadata.custom.quote` in `convert-message`) is unavoidable either way.
6. **Message errors** — text-part routing (`DisplayMessage.type==='error'`) vs native `MessagePrimitive.Error` (needs runtime to populate `message.status`).
7. **Composer queue** — native local `ComposerPrimitive.Queue` (Steer/Remove) vs our **daemon-backed** `QueuedMessageBanner` (edit/cancel by id). Different model; sweep default = keep ours.

## Runtime-gated (do NOT build until the external-store runtime exposes the data)
Reload · Edit/EditComposer · **BranchPicker** (no branches in CLI-resume) · structured Quote round-trip · native error channel. Confirm with the runtime owner first.

## New shadcn components to add (build-order step 2, beyond ToolFallback/ToolGroup)
`quote` (`QuoteBlock`/`SelectionToolbar`/`ComposerQuotePreview`); `reasoning` (gated on decision #4); `thread.tsx`/`message-timing` as layout references only.

## Re-examined: keep-ours corrected (2026-06-05)

**Key framing:** for this codebase it's rarely "keep ours vs adopt native" — it's **native presentational component + our CLI/daemon data + our config-write path.** The native *runtime-integration hooks* (`ModelContext.register`, `useThreadTokenUsage`, `useAssistantInstructions`) are **inert under our external-store runtime** (they target the AI-SDK transport). Use the native *components*; feed them our data; write config via the daemon. **Tally: 6 adopt-native · 9 native-shell+our-data · 9 genuine keep-ours.**

### Flip → ADOPT-NATIVE (was over-conservative)
- Composer attachment tile (`ImageAttachmentPreview`) → shadcn `AttachmentUI`/`ComposerAttachments` (reads the same state our adapter populates; restyle; optional tile-click → our gallery).
- Message image (drop the self-imposed `convert-message.ts:113` `image` skip) → emit `{type:'image', image:data-URL}` parts → shadcn `Image` (built-in zoom); drop `ImageThumbs` single render.
- Message **`ActionBar`** (net-new) — Copy + Export-markdown now; Reload/Edit/Feedback deferred to daemon endpoints (don't render disabled).
- Syntax engine (`SyntaxHighlightedCode` + custom `code` override) → native `components.SyntaxHighlighter` slot via `react-shiki`, fed OUR shiki engine/theme; drop `dangerouslySetInnerHTML`.
- `CodeHeader` → native `components.CodeHeader` slot (props match — pass our component through).

### Native shell + our data (the correct middle path)
- `attachment-adapter` — **IS** the native `AttachmentAdapter` (registered at `adapters.attachments`); not a re-impl. `AttachmentRejectionToaster` — native `unstable_on('attachmentAddError')` + our toast.
- File chip → shadcn `File` (richer: mime icon/size/download) fed our attachment metadata (thread contentType+base64; fallback for replayed history).
- Model picker → `ModelSelector.Root/Trigger/Content` (composable, NOT the default export — its `register` is inert) fed `getModelOptions`; write via `daemonClient.updateChatConfig`. Provider tier reuses the shell.
- Context/token usage → `ContextDisplay.Bar/.Ring` with its **external `usage` prop** (bypasses the inert internal hook) fed our CLI `contextUsage`; drop our bespoke bar.
- Timing footer → shadcn `MessageTiming` fed `metadata.timing` (converter passes it through) + a **Cost** row in `metadata.custom` (native type has no cost slot). Retire `TurnFooter`'s layout.
- @/-mention + /-slash → `ComposerTriggerPopover` + custom `Unstable_TriggerAdapter` (the adapter is **synchronous** → cache+state bridge over async daemon search; use the `action` behavior to write plain `@path`/mention text, not directive chips). Gate on `Unstable_` churn.
- Inline @/slash chips → `createDirectiveText` + custom `Unstable_DirectiveFormatter` (icon map) as the Text component; keep our outer `UserMessage` routing (PLAN card, captures, queued badge, source).

### Genuinely keep ours (survives scrutiny)
- Multi-image **gallery lightbox** (`ImageLightbox`) — native `ImageZoom` is single-image only; ours is shared by `SessionAttachmentsGrid` + 3 todos modals. (Single-image in-message zoom *can* go native.)
- **Sandbox captures** (`CaptureThumb`/`SandboxCaptureContext`) — selector/annotation/color metadata + separate send path; no native slot.
- **Composer drafts** (`composer-drafts`) — runtime doesn't persist the live composer buffer across switches; ours also spans captures+attachments.
- **Effort / Features / PlanMode / permission-mode** — no native concept (`ModelContext` has `callSettings` only). Reskin via shared Popover/Select; logic stays ours.
- **Selected-context** (`ContextPickerMenu` selected items) — `useAssistantInstructions` is a system-prompt injector (inert here); our system prompt is CLI/CLAUDE.md-owned.
- **`ReadMoreBubble`** — assistant-ui ships no read-more/clamp toggle; thin presentation wrapper (doesn't block other adoptions).
- Plus: `use-expandable`, `FindBar`, `MessageRenderBoundary`, `QueuedMessageBanner` (daemon queue), `MainframeRuntimeProvider`/`useChatSession` (app glue), the **global sessions sidebar** (hybrid), the quote→CLI glue.

---

## Master table

<!-- master inventory table appended below -->
| Feature | Native? | Our re-impl | Verdict | Action | Build leaf |
|---|---|---|---|---|---|
| ThreadPrimitive.Root | yes | MainframeThread.tsx:124 (native) | MATCH | Already native; restyle className + tokens only | B1 thread-shell-scaffold |
| ThreadPrimitive.Viewport (+autoScroll engine) | yes | MainframeThread.tsx:125 (native) | MATCH | Already native; restyle (scrollbar-on-hover); optionally adopt new scroll-option props in B2 | B1 thread-shell-scaffold |
| Viewport auto-scroll behavior | yes | none (native engine via Viewport) | MATCH | Do NOT reimplement; verify 0.14.14 tightened handleScroll guards (L111-153) vs use-expandable hack | B2 viewport-footer+scroll |
| ThreadViewport store (isAtBottom/scrollToBottom/inset/turnAnchor) | yes | none (poked indirectly) | MATCH | Use public scrollToBottom/isAtBottom; don't reimplement store | B2 viewport-footer+scroll |
| use-expandable hack (defeat auto-snap on expand) | no | parts/tools/use-expandable.ts:36 | NO-NATIVE | KEEP — no native expand-without-snap; re-test vs 0.14.14, add regression test | B2 viewport-footer+scroll |
| ThreadPrimitive.Messages (children fn vs deprecated components prop) | yes | MainframeThread.tsx:132 (deprecated components prop) | MATCH | Migrate to children render fn; keep MessageRenderBoundary inside | B1 thread-shell-scaffold |
| ThreadPrimitive.MessageByIndex | yes | none | NO-NATIVE | Not needed (FindBar scrolls by DOM); documented as available | B1 thread-shell-scaffold |
| ThreadPrimitive.Empty (deprecated → AuiIf) | yes | MainframeThread.tsx:20 | MATCH | Replace with AuiIf condition s.thread.isEmpty; clears deprecation | B1 thread-shell-scaffold |
| ThreadPrimitive.If (deprecated → AuiIf) | yes | none (useThread().isRunning imperatively) | MATCH | Switch GeneratingIndicator gate to useAuiState/AuiIf isRunning | B1 thread-shell-scaffold |
| AuiIf (declarative conditional) | yes | none | MATCH | Adopt for empty/running gating (replaces Empty + useThread isRunning) | B1 thread-shell-scaffold |
| ThreadPrimitive.ViewportFooter (registers height as inset) | yes | MainframeThread.tsx:145 (manual shrink-0 sibling, no inset) | PARTIAL | Move BottomCard INTO ViewportFooter; fixes tall card overlap + scroll target | B2 viewport-footer+scroll |
| ThreadPrimitive.ViewportProvider | yes | none | NO-NATIVE | Not needed (Viewport is our scroll container); documented | B2 viewport-footer+scroll |
| ThreadPrimitive.ScrollToBottom | yes | none | NO-NATIVE | Add native ScrollToBottom (asChild + our icon, disabled:invisible) — new affordance | B2 viewport-footer+scroll |
| Viewport scroll-option props (OnRunStart/OnInitialize/OnThreadSwitch) | yes | partial — only autoScroll set | MATCH | Defaults already fire via native engine; expose only if product wants suppression | B2 viewport-footer+scroll |
| useThreadViewport / useThreadViewportStore | yes | none (use-expandable DOM-walks + fake scroll event) | PARTIAL | Read isAtBottom via native hook where read-only; DOM hack stays for the flip | B2 viewport-footer+scroll |
| Welcome / empty-state screen | partial | MainframeThread.tsx:20 EmptyState (logo only) | MATCH | No primitive; keep markup, restyle, gate with AuiIf; use shadcn ThreadWelcome as layout ref | B3 welcome+suggestions |
| ThreadPrimitive.Suggestions + SuggestionByIndex | yes | none | NO-NATIVE | Needs SuggestionAdapter (external store has no suggestions field); product decision, don't hand-roll | B3 welcome+suggestions |
| ThreadPrimitive.Suggestion (static button) | yes | none | NO-NATIVE | For static starter prompts use native Suggestion prompt=...send; preferred over hand-rolled buttons | B3 welcome+suggestions |
| turnAnchor='top' (+topAnchorMessageClamp/reserve) | yes | none (default bottom anchor) | MATCH | Opt-in via one prop; deliberate design choice, defer | B4 advanced-scroll |
| LoadingState (chat-store loading gate) | no | MainframeThread.tsx:33 | NO-NATIVE | KEEP — daemon loadingChats concern; restyle only | B1 thread-shell-scaffold |
| GeneratingIndicator (Thinking/Compacting) | no | MainframeThread.tsx:42 | NO-NATIVE | KEEP — native isRunning + our compactingChats; migrate isRunning to useAuiState | B1 thread-shell-scaffold |
| useThread (legacy hook) | yes | MainframeThread.tsx:43, ComposerCard.tsx:63 | MATCH | Replace with useAuiState selectors (deprecation path) | B1 thread-shell-scaffold |
| FindBar (Cmd+F find + scroll-to-match) | no | FindBar.tsx:144 (manual viewport.scrollTo) | NO-NATIVE | KEEP — no native find; ensure it still finds Viewport DOM after shell restructure | B2 viewport-footer+scroll |
| ImageLightbox overlay | no | MainframeThread.tsx:148 | NO-NATIVE | KEEP — app-specific overlay | B1 thread-shell-scaffold |
| MessagePrimitive.Root | yes | AssistantMessage/UserMessage/SystemMessage (native) | MATCH | Already native; restyle; optionally use native isHovering instead of `group` | msg-shell |
| MessagePrimitive.Parts (+web defaults/slots) | yes | AssistantMessage.tsx:27 (native, custom components) | MATCH | Keep native plumbing; fix tools config (by_name) + decide Reasoning | msg-parts / tool-cards |
| Custom Text part (MainframeText) | partial | parts/MainframeText.tsx:8 | PARTIAL | Keep as Text slot; optionally adopt native useSmooth + InProgress cursor inside markdown (low pri) | msg-parts / markdown |
| MessagePartPrimitive.InProgress (streaming cursor) | yes | none (no per-part cursor) | NO-NATIVE | Optional cosmetic add inside MainframeText; defer | msg-parts |
| MessagePartPrimitive.Image | partial | messages/ImageThumbs.tsx | NO-NATIVE | KEEP — custom gallery+lightbox, images from DisplayContent not part stream | msg-parts |
| Reasoning part / ChainOfThought | yes | parts/ThinkingPart.tsx (DISABLED via Reasoning:()=>null) | MISMATCH | STOP-AND-ASK: adopt native Reasoning (shadcn) or keep hidden by product choice; don't keep dead ThinkingPart | msg-parts / markdown |
| MessagePrimitive.Error + useMessageError | partial | parts/ErrorPart.tsx + MainframeText error routing | MISMATCH | STOP-AND-ASK: keep text-part routing (no runtime change) vs migrate to native error channel | msg-parts |
| ErrorPrimitive.Root / .Message | yes | parts/ErrorPart.tsx:8 | NO-NATIVE | Tied to error-channel decision; add role=alert to ErrorPart regardless | msg-parts |
| MessagePrimitive.If (deprecated → AuiIf) | yes | none (plain JS branching) | NO-NATIVE | Don't adopt If; use AuiIf if declarative gating ever needed | msg-shell |
| MessagePrimitive.Attachments / AttachmentByIndex | partial | ImageThumbs FileAttachmentThumbs + UserMessage.tsx:75 | NO-NATIVE | KEEP — attachments from DisplayMessage.metadata, not AttachmentRuntime | msg-parts |
| Role-based message dispatch | yes | MainframeThread.tsx:132 (Messages components map) | MATCH | Already native; keep (System slot is our addition, supported) | msg-shell |
| MessageRenderBoundary (per-message error boundary) | no | messages/MessageRenderBoundary.tsx:11 | NO-NATIVE | KEEP — guards tapClientLookup OOB; re-verify bug on 0.14.14 | msg-shell |
| TurnFooter (timestamp/duration) | partial | messages/TurnFooter.tsx:7 | NO-NATIVE | KEEP — timing from CLI metadata; optionally crib message-timing styling | msg-shell |
| ReadMoreBubble (clamp long user msgs) | no | messages/ReadMoreBubble.tsx:20 | NO-NATIVE | KEEP — assistant-ui never truncates | msg-shell |
| ActionBarPrimitive.Root | yes | none (no action bar exists) | NO-NATIVE | Net-new: adopt native Root (hideWhenRunning/autohide) when building hover-actions | msg-actions |
| ActionBarPrimitive.Copy | yes | none | NO-NATIVE | Net-new; adopt native (clipboard + copied state); add data-testid | msg-actions |
| ActionBarPrimitive.Reload | partial | none | NO-NATIVE | Net-new RUNTIME-GATED: needs runtime.reload(); flag to runtime owner | msg-actions |
| ActionBarPrimitive.Edit | partial | none (no EditComposer) | NO-NATIVE | Net-new RUNTIME-GATED: needs edit/branch; CLI model lacks it; likely defer | msg-actions |
| ActionBarPrimitive.Speak / StopSpeaking | yes | none | NO-NATIVE | Net-new, needs SpeechSynthesisAdapter; defer/drop unless TTS is a goal | msg-actions |
| ActionBarPrimitive.Feedback Positive/Negative | yes | none | NO-NATIVE | Net-new, needs FeedbackAdapter; defer unless feedback sink added | msg-actions |
| ActionBarPrimitive.ExportMarkdown | yes | none | NO-NATIVE | Net-new self-contained (Blob); cheap win in More menu; adopt + restyle | msg-actions |
| ActionBarMorePrimitive (Root/Trigger/Content/Item/Separator) | yes | none | NO-NATIVE | Net-new overflow menu host for ExportMarkdown etc.; adopt when building action bar | msg-actions |
| BranchPickerPrimitive (Root/Next/Prev/Count/Number) | partial | none | NO-NATIVE | Net-new RUNTIME-GATED: single-branch CLI-resume store has no branches; do NOT build until runtime supports | msg-actions |
| EditComposer (inline edit) | partial | none | NO-NATIVE | Net-new RUNTIME-GATED; pairs with Edit + BranchPicker; defer | msg-actions / composer-edit |
| MessagePrimitive.GroupedParts / groupPartByType / indicator slot | yes | DAEMON convert-message.ts synthetic _ToolGroup/_TaskGroup/_TaskProgress + cards | MISMATCH | STOP-AND-ASK: our grouping is server-driven; native is client-side adjacency; coordinate before swap | part-grouping |
| display:'standalone' / standalone-tool promotion | yes | none (cards render inline) | NO-NATIVE | Adopt only if moving to GroupedParts; lets Plan/AskUserQuestion opt out; defer | part-grouping |
| GroupedParts indicator (streaming dot) | yes | ad-hoc pulse dots (TaskCard:62, MCPToolCard:30) | PARTIAL | If GroupedParts adopted, use indicator='no-text'; else keep ours | part-grouping |
| ToolFallback shadcn (collapsible card) | yes | DefaultToolCard + CollapsibleToolCard + ToolResultExpand + shared.tsx | NO-NATIVE | Copy-in reference; keep ours; borrow useScrollLock + shimmer | tool-cards |
| ToolGroup shadcn (group card cva variants) | yes | parts/tools/ToolGroupCard.tsx | NO-NATIVE | Copy-in; keep ours (driven by synthetic _ToolGroup) unless grouping moves | part-grouping |
| legacy components.ToolGroup / ReasoningGroup props | yes | none | NO-NATIVE | Deprecated upstream — do NOT adopt | part-grouping |
| PartPrimitive.Messages (nested subagent thread) | yes | TaskCard (parses <usage>) + TaskGroupCard + pipeline synthetics | MISMATCH | STOP-AND-ASK: native renders real readonly nested thread; needs daemon to attach part.messages | part-grouping |
| tools.by_name (per-name component map) | yes | render-tool-card.tsx switch + Fallback wiring | MATCH | Replace switch with native by_name map (Edit→EditFileCard etc.) | tool-cards |
| tools.Fallback (unregistered tools) | yes | AssistantMessage.tsx Fallback→renderToolCard + DefaultToolCard | MATCH | Point Fallback ONLY at DefaultToolCard (+MCPToolCard); stop using as dispatcher | tool-cards |
| tools.Override (single handler) | yes | none | NO-NATIVE | Not needed (we dispatch per-name); awareness only | tool-cards |
| makeAssistantToolUI registry (setToolUI) | yes | parts/tool-ui-registry.tsx (AllToolUIs) @ provider:313 | MISMATCH | STOP-AND-ASK: cards registered twice; @deprecated upstream → prefer inline by_name, delete registry+mount | tool-cards / tools |
| ToolCallMessagePartProps (status/addResult/resume/respondToApproval) | yes | partial — bespoke ToolCardProps (shared.tsx) drops status/addResult/resume | PARTIAL | On by_name migration pass native props through; wire `status` at minimum | tool-cards |
| useToolArgsStatus (per-arg streaming status) | yes | none (ad-hoc args.x===undefined guards) | NO-NATIVE | Adopt opportunistically in card bodies to avoid flashing partial args | tool-cards / tools |
| MarkdownTextPrimitive (smooth streaming markdown) | yes | parts/markdown-text.tsx:187 (native) | MATCH | Already native; consider componentsByLanguage later | markdown |
| componentsByLanguage (per-lang override, mermaid/diff) | yes | none (single highlighter) | NO-NATIVE | Available via primitive we use; adopt if we want mermaid/diff; no reimpl to remove | markdown |
| unstable_memoizeMarkdownComponents | yes | parts/markdown-text.tsx:162 (native) | MATCH | Already native; keep element overrides | markdown |
| useIsMarkdownCodeBlock | yes | parts/markdown-text.tsx:27 (native) | MATCH | Already native; keep | markdown |
| CodeHeader (lang label + copy) | yes | parts/CodeHeader.tsx | NO-NATIVE | Consumer-supplied; keep ours; type as CodeHeaderProps | markdown |
| code-block-copy helper | yes | CodeHeader.tsx:11 inline clipboard | NO-NATIVE | Local shadcn helper, not a lib export; ours is fine | markdown |
| SyntaxHighlighter override slot + props contract | yes | parts/SyntaxHighlightedCode.tsx (wired via `code` override, not the slot) | PARTIAL | Plug raw-shiki engine into native components.SyntaxHighlighter slot; primitive owns layout | markdown |
| shadcn shiki-highlighter (react-shiki) | yes | SyntaxHighlightedCode.tsx (raw shiki singleton) | NO-NATIVE | Keep ours; do NOT pull react-shiki dep | markdown |
| shadcn syntax-highlighter.ts (Prism alt) | yes | SyntaxHighlightedCode.tsx (shiki) | NO-NATIVE | We chose shiki; ignore this option | markdown |
| MessagePart.Text web default (plain) | yes | MainframeText + markdown-text.tsx | NO-NATIVE | KEEP — we need markdown, not plain default | markdown |
| Reasoning part default (no-op) | yes | AssistantMessage:30 Reasoning:()=>null + ThinkingPart | PARTIAL | Verify ThinkingPart reachable; consider wiring Reasoning slot to ThinkingPart vs null (ties to reasoning mismatch) | markdown / msg-parts |
| ComposerPrimitive.Root | yes | composer/ComposerCard.tsx:287 (native) | MATCH | Already native; restyle className only | composer-shell |
| ComposerPrimitive.Input | yes | ComposerCard.tsx:386 (native + transparent text/custom onKeyDown) | PARTIAL | Keep native Input; transparent-caret + onKeyDown overrides for highlight overlay + queue-on-running stay | composer-shell |
| ComposerPrimitive.Send | yes | ComposerCard.tsx:77 SendButton (custom) | PARTIAL | Use native Send for normal path; keep custom only for captures-only send (native short-circuits on empty) | composer-shell |
| ComposerPrimitive.Cancel | yes | ComposerCard.tsx:62 StopButton (native Cancel) | MATCH | Already native; restyle only | composer-shell |
| ComposerPrimitive.AddAttachment | yes | ComposerCard.tsx:307 (native, Paperclip skin) | MATCH | Already native; restyle | composer-attachments |
| ComposerPrimitive.Attachments + AttachmentByIndex | yes | ComposerCard.tsx:318 (native, ImageAttachmentPreview item) | MATCH | Already native; keep | composer-attachments |
| ComposerPrimitive.AttachmentDropzone | yes | none | MATCH | Adopt native — we have NO drag-drop; pure gain, restyle dragging state | composer-attachments |
| ComposerPrimitive.Quote / QuoteText / QuoteDismiss | yes | QuoteOnSelectionButton.tsx:93 (injects '>' markdown via setText) | MISMATCH | STOP-AND-ASK: native structured quote vs markdown injection; changes wire format + rendering | composer-quote / quote-selection |
| composer.quote / setQuote scope + useMessageQuote + QuoteMessagePart | yes | none (quote lives as plain text) | MISMATCH | Tied to Quote decision; gives structured render on message side | composer-quote / quote-selection |
| ComposerPrimitive.Queue + QueueItem (Text/Steer/Remove) | yes | composer/QueuedMessageBanner.tsx:70 (daemon-backed) | NO-NATIVE | KEEP — native is local in-browser queue; ours is server-side by messageId; 'Steer' no analog | composer-queue |
| ComposerPrimitive.If | yes | none | NO-NATIVE | Unused helper; optional cleaner conditionals; low value | composer-shell |
| ComposerPrimitive.Dictate / StopDictation / DictationTranscript | yes | none | NO-NATIVE | No voice feature; needs DictationAdapter; note only | composer-shell |
| AttachmentPrimitive.Root | yes | ImageAttachmentPreview.tsx:28 (native) | MATCH | Already native; restyle | composer-attachments |
| AttachmentPrimitive.Remove | yes | ImageAttachmentPreview.tsx:44 (native, X skin) | MATCH | Already native; restyle | composer-attachments |
| AttachmentPrimitive.Name | yes | ImageAttachmentPreview.tsx:52 (custom span) | MATCH | Swap to native Name (asChild) as cleanup; trivial | composer-attachments |
| AttachmentPrimitive.unstable_Thumb | yes | ImageAttachmentPreview.tsx:30 (custom img + lightbox) | PARTIAL | Electron lightbox justifies custom; could use native Thumb + our onClick wrapper | composer-attachments |
| AttachmentAdapter interface | yes | composer/attachment-adapter.ts:20 (5MB, accept='*', dataURL) | MATCH | Correct extension point; keep; accept='*' documents fileMatchesAccept '*/*' bug | composer-attachments |
| Simple/Composite AttachmentAdapters (prebuilt) | yes | attachment-adapter.ts (single custom) | NO-NATIVE | Keep custom (5MB guard + unified image+doc); note they exist | composer-attachments |
| attachmentAddError event | yes | AttachmentRejectionToaster.tsx:19 (native event + custom toast) | MATCH | Keep — exemplary native-plumbing/custom-content | composer-attachments |
| Unstable_TriggerPopover + mention/slash adapters | yes | chat/ContextPickerMenu.tsx (full hand-rolled @/files+/skills+agents) | NO-NATIVE | Biggest reimpl; native is unstable_ + model-context coupled; keep ours, flag future migration | composer-mentions |
| edit-composer (edit sent user msg) | yes | none | NO-NATIVE | Feature gap not reimpl; native EditComposer exists; note only | composer-edit |
| Composer syntax/mention highlight overlay | no | composer/ComposerHighlight.tsx | NO-NATIVE | KEEP — native ComposerInput has no highlighting | composer-shell |
| Composer config toolbar (model/permission/effort/features/plan/worktree) | no | ComposerCard.tsx:411 + ComposerDropdown/EffortPicker/FeaturesPopover/PlanModeToggle/WorktreePopover | NO-NATIVE | OURS by design; keep entirely | composer-toolbar |
| Sandbox capture thumbnails + context preamble | no | ComposerCard.tsx:324 CaptureThumb + SandboxCaptureContext | NO-NATIVE | OURS — no native captures concept; reason native send is bypassed for captures-only | composer-attachments |
| Composer draft persistence/restore | no | ComposerCard.tsx:158 + composer/composer-drafts.ts | NO-NATIVE | OURS — no native draft persistence across chat switches | composer-shell |
| ThreadListPrimitive.Root | yes | panels/ChatsPanel.tsx (Zustand-driven) | MISMATCH | STOP-AND-ASK: global cross-project panel vs per-runtime thin div; needs hoisted thread-list runtime | thread-list |
| ThreadListPrimitive.Items (render-prop loop) | yes | ChatsPanel.tsx:653 filteredFlatChats.map + buildGroups/ProjectGroup | MISMATCH | STOP-AND-ASK: no native project-grouping/filter; keep ours unless runtime hoisted | thread-list |
| ThreadListPrimitive.ItemByIndex | yes | none | NO-NATIVE | Not needed; only if native Items adopted | thread-list |
| ThreadListPrimitive.New | yes | ChatsPanel.tsx:490 (new-session btn + NewSessionPopover) | MISMATCH | Domain needs project selection first; keep ours | thread-list |
| ThreadListPrimitive.LoadMore | yes | none (all chats in Zustand, no pagination) | NO-NATIVE | No pagination today; awareness only | thread-list |
| ThreadListItemPrimitive.Root | yes | panels/FlatSessionRow.tsx:275 (isActive via Zustand) | MISMATCH | STOP-AND-ASK: active from Zustand vs aui store; keep ours unless runtime hoisted | thread-list |
| ThreadListItemPrimitive.Trigger | yes | FlatSessionRow.tsx:324 (setActiveChat + openChatTab + resumeChat) | MISMATCH | Native has no slot for tab + daemon side effects; keep ours | thread-list |
| ThreadListItemPrimitive.Title | yes | FlatSessionRow.tsx:333 (title span) | MISMATCH | Ours has tooltip + unread/active weighting; keep unless runtime hoisted | thread-list |
| ThreadListItemPrimitive.Archive | yes | FlatSessionRow.tsx:479 (worktree-delete confirm + removeChat + ...) | MISMATCH | Native button can't express worktree/tab/activate-next; keep ours | thread-list |
| ThreadListItemPrimitive.Unarchive | yes | panels/ArchivedSessionsPopover.tsx | MISMATCH | We surface via dedicated popover (worktree restore); keep ours | thread-list |
| ThreadListItemPrimitive.Delete | yes | none (archive only) | NO-NATIVE | Intentionally no per-chat hard delete; awareness only | thread-list |
| ThreadListItemMorePrimitive | yes | ChatsPanel.tsx:377 ContextMenu (Tags/Rename/Pin/Archive/Copy ID) + hover btns | MISMATCH | Richer right-click + inline model; keep ours | thread-list |
| ExternalStoreThreadListAdapter wiring | yes | MainframeRuntimeProvider.tsx:219-264 (DEAD — never rendered) | MISMATCH | STOP-AND-ASK: delete dead adapter OR commit to native thread-list + hoist runtime | thread-list |
| SelectionToolbarPrimitive.Root | yes | QuoteOnSelectionButton.tsx:51 (bespoke listeners/placement) | MATCH | Replace listeners/positioning with native Root; restyle; data-message-id already present | quote-selection |
| SelectionToolbarPrimitive.Quote | yes | QuoteOnSelectionButton.tsx:93 (quotify→setText) | PARTIAL | Adopt native Quote (calls setQuote); drop quotify+setText; PARTIAL — send path must serialize quote→CLI text | quote-selection |
| getSelectionMessageId util | yes | QuoteOnSelectionButton.tsx:16 (thread guard, no messageId) | PARTIAL | Adopt native for per-message attribution; free with SelectionToolbar.Root | quote-selection |
| MessagePrimitive.Quote (in-message quote block) | yes | none (raw '>' markdown in body) | PARTIAL | Adopt for styled quote block; needs send + convert-message to round-trip metadata.custom.quote | quote-selection / msg-quote |
| useMessageQuote / getMessageQuote | yes | none | NO-NATIVE | Pairs with MessagePrimitive.Quote; use if rendering quote blocks | quote-selection / composer |
| QuoteBlock shadcn (Root/Icon/Text) | yes | none (markdown renderer) | MATCH | Vendor + restyle to mf tokens; pass to MessagePrimitive.Quote | quote-selection |
| SelectionToolbar shadcn (composed Root+Quote) | yes | QuoteOnSelectionButton.tsx (whole file) | MATCH | Replace with vendored shadcn SelectionToolbar; delete QuoteOnSelectionButton after migration | quote-selection |
| ComposerQuotePreview shadcn (Root/Icon/Text/Dismiss) | yes | none | MATCH | Vendor + restyle; place in ComposerCard as dismissable chip above input | quote-selection / composer-quote |
| Quote round-trip on send/persist (metadata.custom.quote ↔ CLI text) | partial | MainframeRuntimeProvider.tsx:112 onNew (no quote) + convert-message.ts (no extract/restore) | MISMATCH | STOP-AND-ASK glue: serialize quote→CLI text on send + restore metadata on resume so Quote block survives; unavoidable even with native adoption | quote-selection |
| useExternalStoreRuntime (the runtime) | yes | MainframeRuntimeProvider.tsx (native) | MATCH | Already native; bump only, no reimpl | runtime-provider |
| MainframeRuntimeProvider app context (permission/lightbox/captures/error) | no | MainframeRuntimeProvider.tsx (MainframeRuntimeContext) | NO-NATIVE | KEEP — no native equivalent; sits on top of AssistantRuntimeProvider | runtime-provider |
| useChatSession (daemon session glue) | no | hooks/useChatSession.ts | NO-NATIVE | KEEP — daemon WS/REST plumbing | runtime-provider |
| useAui (root accessor) | yes | none | MATCH | Adopt as target for new runtime reads; replaces deprecated accessors | runtime-provider |
| useAuiState (reactive selector) | yes | none | MATCH | Use for all state reads; select fields (whole-state throws) | runtime-provider |
| useAuiEvent (event subscription) | yes | none | NO-NATIVE | Consider for thread-switch/modelContext reactions vs ad-hoc store subscribes | runtime-provider |
| useThread / useThreadRuntime (DEPRECATED) | yes | MainframeThread.tsx:43, ComposerCard.tsx:63 | MATCH | Migrate to useAuiState((s)=>s.thread.isRunning) | thread |
| useThreadComposer / useThreadModelContext (DEPRECATED) | yes | none | MATCH | Not used; prefer useAuiState((s)=>s.thread.composer) | thread |
| useMessage / useMessageRuntime (DEPRECATED) | yes | AssistantMessage:13, UserMessage:30, SystemMessage:15, TurnFooter:8, MainframeText:29 | MATCH | Migrate 5 call sites to useAuiState((s)=>s.message)/getExternalStoreMessages | message |
| useComposerRuntime | yes | QuoteOnSelectionButton + ComposerCard (7 files) | MATCH | NOT deprecated (useComposer is); keep; never useComposer() | composer |
| useThreadListItem / Runtime + Provider | yes | none (threadList via external-store adapter) | MATCH | If per-item runtime UI ever needed, use useAuiState((s)=>s.threadListItem) | thread-list |
| getExternalStoreMessages / bindExternalStoreMessage | yes | none (consumed natively) | MATCH | Keep; verify only getExternalStoreMessages (bind* is deprecated/experimental) | message |
| makeAssistantTool (DEPRECATED tool-def) | yes | none | MATCH | Not used; for future client tools use defineToolkit + Tools() | tools |
| defineToolkit / Tools / Toolkit (post-deprecation model) | yes | none | NO-NATIVE | Supported replacement when makeAssistantToolUI removed; track for tool-cards migration | tools |
| useAssistantInstructions / model-context | yes | none (instructions in CLI/CLAUDE.md) | NO-NATIVE | Intentionally unused; do not adopt | runtime-provider |
| ReadonlyThreadProvider (static viewer) | yes | none | NO-NATIVE | Candidate for any non-interactive transcript surface | thread |
| useThreadViewportAutoScroll (stick-to-bottom) | yes | use-expandable.ts (fights it), FindBar.tsx (manual) | PARTIAL | Read viewport store instead of dispatching synthetic scroll; FindBar can stay but consult store | thread / B2 |

## Corrected verdict table (keep-ours re-examined)

| Feature | Native component | Data fit | Revised verdict | Action |
|---|---|---|---|---|
| Composer attachment adapter (5MB cap, dataURL, accept-all, image/doc typing) | AttachmentAdapter interface (core/src/adapters/attachment.ts:9) | Perfect — IS the native extension point, registered at adapters.attachments (provider:261) | NATIVE-SHELL+OUR-DATA | Keep adapter body as the sanctioned plug-in. Stop calling it a reimpl. |
| Composer pending-attachment tile (thumb + remove + caption) | shadcn attachment.tsx AttachmentUI/ComposerAttachments (:126/:198) | Reads the same composer state our adapter fills — zero glue | ADOPT-NATIVE | Replace ImageAttachmentPreview; restyle + testids. Override click -> our lightbox or keep native Dialog. |
| Message-side image display (thumbnail grid) | shadcn image.tsx Image + MessagePart Image (:422) | Self-imposed blocker (convert-message.ts:113 skips 'image') | ADOPT-NATIVE | Stop skipping 'image'; emit data:URL image parts; render via native Image (built-in zoom). Drop ImageThumbs single-image. |
| Multi-image lightbox WITH gallery nav (prev/next, counter, arrow keys) | Partial — ImageZoom (image.tsx:245) single-image only | Native has no array/index/onNavigate; shared by SessionAttachmentsGrid + 3 todos modals | KEEP-OURS | Keep ImageLightbox for gallery + non-chat consumers. Single-image in-message zoom may use native ImageZoom. |
| Message-side file/document chip | shadcn file.tsx File (:190/:209) | Strictly richer (mime icon, size, Download); need contentType + base64 threaded onto message | NATIVE-SHELL+OUR-DATA | Adopt File; thread contentType+dataURL in convert-message. Fall back to Root+Icon+Name for name-only history. |
| Attachment-add rejection toast | Native event (composer.unstable_on('attachmentAddError')) — no UI | Exact fit; AttachmentRejectionToaster already subscribes (line 19) | NATIVE-SHELL+OUR-DATA | Keep as-is. Reclassify to native-event-driven, not reimpl. |
| Sandbox capture thumbnail (selector/annotation/color metadata) | none | Captures aren't Files/image-parts; separate sendCaptures path | KEEP-OURS | Keep CaptureThumb/SandboxCaptureContext entirely. No native analog. |
| Model selector (model tier) | ModelSelector.Root/Trigger/Content (model-selector.tsx:64) | Strong; default export's modelContext().register is INERT under external store | NATIVE-SHELL+OUR-DATA | Use composable parts (not default export); feed getModelOptions, write via updateChatConfig. |
| Adapter/provider selector (provider tier) | none (single flat tier) | Shell-reusable for parity; no provider+model semantics | NATIVE-SHELL+OUR-DATA | Optional: reuse ModelSelector.Root shell. Adapter-change + reset logic stays ours. |
| Effort / reasoning-level picker | none (callSettings has no effort field) | No semantic fit; only generic dropdown chrome | KEEP-OURS | Keep EffortPicker; at most reskin via shared dropdown primitive. |
| Features / per-model toggles popover | none (no feature-flag map) | No fit; borrow generic Popover chrome only | KEEP-OURS | Keep FeaturesPopover; optionally shared Popover host. |
| Plan-mode toggle | none | No native target; already shared button+Tooltip | KEEP-OURS | Keep as-is. |
| Permission-mode selector | none (no permission-mode concept) | Only dropdown chrome shareable | KEEP-OURS | Keep; reskin via shared Select if unifying chrome. |
| Context-window / token-usage display | ContextDisplay Ring/Bar/Text (context-display.tsx:164) | Strong — accepts EXTERNAL usage prop, bypasses internal fetch | NATIVE-SHELL+OUR-DATA | Adopt ContextDisplay.Bar with usage from CLI contextUsage + modelContextWindow. Drop bespoke bar. |
| Selected-context / attached-instructions | useAssistantInstructions/ContextProvider | Misfit — system-prompt injector, inert under external store; not a chip UI | KEEP-OURS | Keep inline @-text + addMention. Do not adopt. |
| @-mention / /-slash picker popover | ComposerTriggerPopover + Unstable_TriggerAdapter (trigger.ts:7) | Partial — adapter is SYNCHRONOUS (no Promise); CLI wants plain @path not directive chips | NATIVE-SHELL+OUR-DATA | Prototype with custom adapter (sync cache bridge + `action` behavior). Gate on Unstable_ churn. |
| Per-chat composer draft persistence | none (runtime persists thread metadata, not composer buffer) | No target; draft spans captures+attachments | KEEP-OURS | Keep composer-drafts.ts. |
| Per-turn timing/stats footer | MessageTiming shadcn (message-timing.tsx:38) + hook | Feedable via metadata.timing passthrough; native type has NO cost slot (message.ts:253-257) | NATIVE-SHELL+OUR-DATA | Adopt component; feed CLI usage; edit copied file to drop TTFT/Chunks, add Cost row. Retire TurnFooter layout. |
| Message action bar (copy/reload/edit/export/feedback) | ActionBarPrimitive (actionBar/*) | Copy+Export need zero backend; Reload/Edit/Feedback/Speak need daemon support we lack | ADOPT-NATIVE | Adopt net-new with Copy+ExportMarkdown now; autohide=not-last+float; defer the rest (don't render disabled). |
| Inline directive/slash/mention chips | createDirectiveText(formatter,{iconMap}) (directive-text.tsx:19) | Chip layer fits; outer routing (plan/sandbox/queued/source) has no native equiv | NATIVE-SHELL+OUR-DATA | Adopt chip renderer with custom formatter+iconMap; keep our UserMessage routing shell. |
| Clamp + Read more/Show less (long user msgs) | none (confirmed via grep) | No native component; pure client-side text measure | KEEP-OURS | Keep ReadMoreBubble. Pure styling — doesn't block other adoptions. |
| Code-block syntax highlighting engine | SyntaxHighlighter slot -> react-shiki (shiki-highlighter.tsx:30) | No CLI data — markdown text only; our shiki 4.0.2 engine plugs in via `highlighter` prop | ADOPT-NATIVE | Use SyntaxHighlighter slot with our createHighlighterCore + `delay`. Add react-shiki dep; drop dangerouslySetInnerHTML. |
| Raw shiki singleton (engine/streaming) | react-shiki `highlighter` + `delay` | Direct — our singleton IS a createHighlighter; `delay` supersedes our re-highlight | ADOPT-NATIVE | Build core once, hand to react-shiki; use `delay` for streaming. |
| CodeHeader (lang label + copy) | CodeHeader slot (markdown-text.tsx:30) | Perfect contract match (CodeHeaderProps {language,code}) | ADOPT-NATIVE | Pass our existing CodeHeader through components.CodeHeader. No rewrite; testid+tests stay green. |
