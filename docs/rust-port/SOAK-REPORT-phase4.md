# Phase-4 LIVE Soak Report (Node vs Rust daemon, real claude CLI)

Generated by `packages/core-rs/tools/diffd/soak.mjs`. Each daemon booted on a private temp data dir; scenarios drove the **real** claude CLI over WS + HTTP. Structure (ordered event types + normalized payload fields) is compared; the parity scenario also compares assistant text.

- claude CLI: `2.1.206 (Claude Code)`
- Node model: `sonnet` (cheapest advertised); adapter.installed probe: `true`
- Rust model: `sonnet`; adapter.installed probe: `true`

## Blockers

- Node daemon build failed; fell back to the prebuilt packages/core/dist/index.js.

## Verdicts

| Scenario | Verdict | Detail |
|---|---|---|
| parity-text | DIVERGENT | event seq .length: 21 → 20 |
| tool-permission | DIVERGENT | event seq .length: 27 → 26 |
| interrupt | DIVERGENT | event seq .length: 17 → 16 |

Verdicts: **STRUCTURE-MATCH** (event-type sequence + normalized fields equal), **DIVERGENT** (a real structural/field difference — needs a fix), **BLOCKED(rust/node)** (that daemon could not run the scenario), **SKIPPED** (CLI/auth unavailable).

## Scenario: parity-text

**Verdict:** DIVERGENT. event seq .length: 21 → 20

**Node event stream:** `connection.ready adapter.models.updated adapter.models.updated message.queued.snapshot subscribe:ack process.started message.added display.messages.set chat.updated chat.updated process.ready message.queued.processed message.added display.message.added message.queued.snapshot chat.updated message.added display.message.updated chat.notification chat.contextUsage chat.updated`

**Rust event stream:** `connection.ready adapter.models.updated message.queued.snapshot subscribe:ack process.started message.added display.messages.set chat.updated chat.updated process.ready message.queued.processed message.added display.message.added message.queued.snapshot chat.updated message.added display.message.updated chat.notification chat.contextUsage chat.updated`

**Node assistant text:** `Reply with exactly: PARITY_OK — no tools, no thinking.PARITY_OKPARITY_OKPARITY_OK` (PARITY_OK: true)

## Scenario: tool-permission

**Verdict:** DIVERGENT. event seq .length: 27 → 26

**Node event stream:** `connection.ready adapter.models.updated adapter.models.updated message.queued.snapshot subscribe:ack process.started message.added display.messages.set chat.updated chat.updated process.ready message.queued.processed message.added display.message.added message.added display.message.updated message.added display.message.updated message.added display.message.updated message.queued.snapshot chat.updated message.added display.message.updated chat.notification chat.contextUsage chat.updated`

**Rust event stream:** `connection.ready adapter.models.updated message.queued.snapshot subscribe:ack process.started message.added display.messages.set chat.updated chat.updated process.ready message.queued.processed message.added display.message.added message.added display.message.updated chat.updated message.added display.message.updated message.added display.message.updated message.queued.snapshot chat.updated message.added display.message.updated chat.notification chat.contextUsage`

**Node permission tool:** `undefined`, permission.resolved seen: false

## Scenario: interrupt

**Verdict:** DIVERGENT. event seq .length: 17 → 16

**Node event stream:** `connection.ready adapter.models.updated adapter.models.updated message.queued.snapshot subscribe:ack process.started message.added display.messages.set chat.updated chat.updated process.ready message.queued.processed message.queued.snapshot chat.updated message.added chat.contextUsage chat.updated`

**Rust event stream:** `connection.ready adapter.models.updated message.queued.snapshot subscribe:ack process.started message.added display.messages.set chat.updated chat.updated process.ready message.queued.processed message.queued.snapshot chat.updated message.added chat.contextUsage chat.updated`

**Node interrupt:** POST /interrupt → HTTP 200, process.stopped seen: false

## Post-review fix analysis (adversarial findings)

Three adversarial-review findings targeted this soak; all three moved the deltas from
`-2` to `-1` and eliminated the structural chat.updated divergence:

- **Adapter connect-replay (was: 0 replay events).** `handle_socket` now streams
  `build_connect_replay_events(ctx.adapter_registry.get_snapshots())` right after
  `connection.ready`, mirroring `buildConnectReplayEvents`. Rust now replays every
  **probed** adapter — visible as the leading `adapter.models.updated` on every
  scenario (previously absent).
- **`adapter.models.updated` broadcast (claimed missing).** Refuted: `apply_refresh`
  already emits it (`lib.rs`). The soak's `-2` was entirely the missing connect-replay
  above; the live probe broadcast fires at boot, before any client connects, so only
  the replay is observable to a scenario client.
- **`chat.updated` finalization ordering.** `send_message` was **awaiting**
  `do_generate_title` (a CLI shell-out); TS fires it `.catch()`-style without awaiting.
  Now spawned. The title `chat.updated` moved out of the early cluster: parity-text and
  interrupt now match Node exactly (`… chat.contextUsage chat.updated` trailing, two
  early updates). tool-permission lands the async title update mid-turn instead of
  trailing — inherent nondeterminism of a fire-and-forget shell-out (the chat.updated
  **count** matches Node 4↔4); this now mirrors Node's own async title semantics.

**Residual `-1` (all scenarios): codex catalog stays `fallback` in Rust.** Node replays
two `adapter.models.updated` (claude + codex); Rust replays one (claude). Standalone the
Rust daemon leaves codex at `catalogSource: fallback / 0 models` even after 45s despite
`codex` being installed — `CodexAdapter::list_models` (temp app-server + `model/list`)
yields no live catalog in this environment, so codex is never `probed` and is correctly
excluded from the replay. This is a separate codex-adapter probe matter, pre-existing and
outside the connect-replay/ordering findings; the replay wiring itself is faithful (it
replays exactly the probed adapters, as Node does).

