//! Ported from `src/server/middleware/*`.

pub mod auth;

// PORT STATUS: src/server/middleware/* (auth only)
// confidence: high
// todos: 0
// notes: auth.ts is the only middleware in the TS tree.
