{
  "name": "@qlan-ro/mainframe-e2e",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build:app:tauri": "pnpm --filter @qlan-ro/mainframe-types build && cargo build --release -p mainframe-daemon --manifest-path ../core-rs/Cargo.toml && VITE_DAEMON_PORT=${MF_E2E_DAEMON_PORT:-31416} pnpm --filter @qlan-ro/mainframe-ui build",
    "test": "playwright test",
    "test:debug": "playwright test --debug",
    "test:headed": "playwright test --headed",
    "test:file": "playwright test",
    "test:mock": "E2E_MODE=mock playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1",
    "@types/node": "^25.9.5",
    "dotenv": "^17.4.2",
    "typescript": "^6.0.3"
  }
}
