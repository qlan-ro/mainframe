{
  "name": "@qlan-ro/mainframe-e2e",
  "version": "0.1.1-rc.1",
  "private": true,
  "type": "module",
  "scripts": {
    "build:app:tauri": "pnpm --filter @qlan-ro/mainframe-types build && cargo build --release -p mainframe-daemon --manifest-path ../core-rs/Cargo.toml && VITE_DAEMON_PORT=${MF_E2E_DAEMON_PORT:-31416} pnpm --filter @qlan-ro/mainframe-ui build",
    "test": "E2E_MODE=mock playwright test",
    "test:debug": "E2E_MODE=mock playwright test --debug",
    "test:headed": "E2E_MODE=mock playwright test --headed",
    "test:file": "E2E_MODE=mock playwright test",
    "test:real": "MF_E2E_ALLOW_REAL_ADAPTER=1 playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1",
    "@types/node": "^26.1.1",
    "dotenv": "^17.4.2",
    "typescript": "^6.0.3"
  }
}
