{
  "name": "@qlan-ro/mainframe-e2e",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build:app": "VITE_DAEMON_HTTP_PORT=${MF_E2E_DAEMON_PORT:-31416} VITE_DAEMON_WS_PORT=${MF_E2E_DAEMON_PORT:-31416} pnpm --filter @qlan-ro/mainframe-desktop build && pnpm --filter @qlan-ro/mainframe-core build",
    "test": "playwright test",
    "test:debug": "playwright test --debug",
    "test:headed": "playwright test --headed",
    "test:file": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0",
    "@types/node": "^25.5.0",
    "dotenv": "^17.3.1",
    "typescript": "^6.0.3"
  }
}
