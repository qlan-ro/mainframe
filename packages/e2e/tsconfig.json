{
  "compilerOptions": {
    "target": "ES2022",
    "module": "NodeNext",
    "moduleResolution": "NodeNext",
    "strict": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "outDir": "dist"
  },
  "include": ["**/*.ts"],
  "comment": "plugins/mock-cli is a standalone esbuild bundle with its own tsconfig (bundler resolution); excluded here so the Playwright NodeNext program doesn't choke on its extensionless imports.",
  "exclude": ["dist", "node_modules", "plugins/mock-cli"]
}
