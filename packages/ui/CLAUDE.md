# `@qlan-ro/mainframe-app-tauri`

The new **Tauri 2 + React** desktop UI — a *parallel* rebuild of `packages/app-electron` (Electron), not a modification of it. Treat `desktop/` as **reference only** (behavior + the visual target); never edit it from here.

> **New here? Read `docs/architecture/MIGRATION-INDEX.md` first** — it catalogs every doc, the vendored design spec (`docs/design-reference/`), and the flow. Then the root `CLAUDE.md` (monorepo code rules), the runtime ADR (`2026-06-05-chat-runtime-decision.md`), the tracker (`MIGRATION-TRACKER.md`), and the architecture/critique docs.

---

## ⭐ Golden rule: chat/thread/thread-list components — assistant-ui first, **stop-and-ask on mismatch**

**Layering (user decision, 2026-08-06):** **assistant-ui owns state and behavior; shadcn owns pixels.** aui
primitives render INTO shadcn chat markup; aui's styled registry components contribute nothing visual. When
both libraries "have" a component it is almost always behavior-vs-look — compose them rather than choosing.

For **any chat / thread / thread-list / message / tool-card / permission / composer component**, follow this process — never skip it:

1. **Research** what assistant-ui provides for it. Check the **latest published release** — not just our pinned/installed version — because the API moves fast and a newer component may justify a bump; **and** check the *installed source* for what we actually have today (docs drift from both). Look in the package exports AND the shadcn **registry** (`@assistant-ui/react-ui` / `npx shadcn add` components like `model-selector`), which ship components into our repo rather than as imports. For each candidate, confirm whether its runtime wiring (e.g. `ModelContext`, `useAssistantInstructions`) is **inert under our `useExternalStoreRuntime`** — if so it's a presentational shell only and we keep our own daemon write path.
2. **Compare** it to our warm-chrome prototype design (the relevant artboard).
3. **If it matches** → use the assistant-ui component, **restyled** to the design. Do not rebuild what it provides.
4. **If it does NOT match** → **STOP. Build neither version yet.** Present a concise **side-by-side summary — our design vs the native assistant-ui component** (what each offers, where they diverge, the cost of each direction) and **ask for a decision.**

Never silently (a) rebuild what assistant-ui already provides, nor (b) force a native component that doesn't fit the design — a mismatch is a **human decision**. *(Subagents can't prompt the user: on a mismatch, STOP, build nothing, and return the design-vs-native summary to the orchestrator, who asks.)*

The per-area pointers below are where to look in step 1:

- **Tool cards → MATCH (use the engine wholesale, restyle).** `MessagePrimitive.GroupedParts` + `tools.by_name`/`Fallback`; restyle the shadcn `ToolFallback`/`ToolGroup` compounds; subagent transcripts via `MessagePartPrimitive.Messages` (+ `ReadonlyThreadProvider`); per-arg streaming via `useToolArgsStatus`. Build per-family cards (Edit/Write/Bash/Read/Grep/Todo/Web/MCP/Plan/Skill/Worktree/Schedule) as `tools.by_name` entries, `ToolFallback` as the catch-all. **DROP** `makeAssistantToolUI` (deprecated) → one `tools.by_name` map (`ToolFallback` = catch-all). **DECIDED: go native** — `GroupedParts` + `groupPartByType` + `display:'standalone'` for tool grouping; `part.messages` (+ `ReadonlyThreadProvider`) for subagent/Task transcripts. **Preferred path = do it in the projection:** `convert-message` maps the daemon's *existing* nested encoding → native ThreadMessage parts (flat groupable tool-calls + a Task tool-call carrying `messages`), so **no daemon/mobile-contract change** if the nested payload is sufficient; daemon flat-parts is the fallback only if it isn't (verify at build). See `docs/architecture/ASSISTANT-UI-INVENTORY.md` #1/#2.
- **Permissions / ask / plan → DONE: inline gate cards, hybrid (native inline parts + our `extras` reply).** Correction to the earlier note: assistant-ui's native tool **`approval`** gate (the tool-call `approval` field + the `respondToApproval` prop, backed by the external-store adapter `onRespondToToolApproval`) **IS usable** under our `useExternalStoreRuntime` — it's data-driven, not transport-coupled. Only the separate `hitlTool()`/`interrupt:{type:'human'}`/`resume()` path is inert (needs a "use generative" compiler). We bypass the native `approval` plumbing **by choice** because our gate data is out-of-band (`control_request` → `extras.permissions`), not in the message stream. Shipped: 3 prop-driven cards (`PermissionGate`/`AskUserQuestionGate`/`PlanGate`) under `features/chat/gates/`, dispatched by `ControlRequest.toolName` from a single `ChatGateMount` mounted inline at the thread tail (`ChatThread.tsx`), reading the queue-front via `useChatPermissionFront` (sorts `extras.permissions` by `askedAt`) and replying through `replyToPermission`. Permission cards dismiss on answer; AskUserQuestion/Plan persist via the existing tool-result display cards. Queue-front-only (one gate at a time); Always-Allow only when `suggestions.length>0`; **don't invent** risk levels/scopes/per-arg toggles. **Reply seam (judo-C):** `replyToPermission`/`ReplyFn` take ONLY the `ControlResponse` — it carries its own `requestId`, so there is no separate arg to desync. **Delivery is verified:** answering optimistically drops the gate, then a 3s re-read of the daemon's pending **restores** it if the `permission.respond` was dropped (closed socket); a restore on subscribe/reconnect (`GET /pending-permission`) **skips** a tool use we just answered. Plan: `docs/plans/2026-06-05-interactive-chat-gate-cards.md`; memory: `aui-approval-gate-external-store`.
- **Composer → MATCH (~90% native, restyle).** `ComposerPrimitive` Root/Input/Send/Cancel (`asChild`→our icon buttons, swap on `thread.isRunning` via `AuiIf`)/AddAttachment/AttachmentDropzone/Attachments/Quote. **Keep `QueuedUserTurn` (daemon-backed); the native `Queue` is a different local model — do NOT use it.** Build the config toolbar (model+effort+features in one menu, plan, permission, worktree) as **stateless** shadcn children that **PATCH the daemon directly** — `/api/chats/:id/config` (model/plan/permission) + `/tuning` (effort/features), Zod-validated daemon-side — NOT `composer.setRunConfig` (that targets the AI-SDK transport, inert under our external-store runtime). **Config is server-authoritative with NO optimistic edits** (judo-B): the controller owns it (seeds from REST on load, mirrors every `chat.updated` into `state.chatConfig`); `useComposerTuning` reads `extras.state.chatConfig` live and a control just fires the PATCH — the daemon's broadcast updates the toolbar. This matches the desktop client and avoids the optimistic-vs-broadcast flicker. **`/`-skills + `@`-file pickers → DONE: native `Unstable_TriggerPopover` (reversal of the earlier shadcn-`Command` call).** Both triggers live on one `Unstable_TriggerPopoverRoot` wrapping the composer (`features/chat/composer/triggers/`): `/` (skills, sync adapter over a per-chat `SkillsProvider`) and `@` (files, an async-over-sync adapter — debounced `searchFiles` into a cache the sync adapter reads). Insertion uses a **custom literal directive formatter** (`serialize → "<prefix><id> "`, `parse → all-text`) so the sent message carries plain `/skill` / `@path` text the CLI parses — NOT directive chips. `@` is **insert-text-only** (no `POST /mentions`). Adapter types are local structural copies (`@assistant-ui/core` isn't resolvable; item/formatter types come from `@assistant-ui/react`). ⚠️ `Unstable_` API — exact-pinned (`@0.14.27` as of 2026-07); coupling kept in the small adapters. Custom thumbnail (native `Thumb` is @alpha/extension-only). **Never `useComposer()`** (getSnapshot loop) — `useAuiState` + `useAui().composer()`. **New-thread flow is draft-aware (D3 preserved):** the picker is skipped when a project pill is active; the live adapter/model/permission/plan/effort/feature selectors are usable on the `__LOCALID_*` draft before the first send; the chat is still created on the **first send only** (no empty sessions). **Known follow-up: provider-tuning-defaults are not yet fetched** — `ModelMenuRow`/`use-composer-tuning` pass `undefined` for the provider-defaults arg, so controls resolve model-effort/feature constraints without provider inheritance until a settings/provider-defaults fetch lands (tracked in the tracker backlog).
- **Quote / select-to-quote → MATCH (use native).** `SelectionToolbar` (floating quote button on text selection — **replaces our `QuoteOnSelectionButton`**), `MessagePrimitive.Quote` + `QuoteBlock` (inline quote in the user message), `ComposerPrimitive.Quote/QuoteText/QuoteDismiss` + `ComposerQuotePreview` (composer pill). Install the shadcn `quote` component and restyle; don't re-implement selection/quote handling.
- **Sessions / thread-list → HYBRID (decided).** One global `useRemoteThreadListRuntime` (all sessions; domain data in thread `custom`: projectId/tags/prState/worktree/status/pinned) + native `ThreadListItemPrimitive` rows (rename/archive/delete/select/active) rendered inside **our** grouped/filtered/pinned sidebar layout via `ThreadListItemRuntimeProvider`/`ThreadListItemByIndexProvider`. NOT the flat default `ThreadListPrimitive.Items`; NOT per-project runtimes. **BUILT (step 11).** Verdicts: D10 — native Archive kept; deleteWorktree choice resolved inside adapter.archive via the archive-confirm bridge (ArchiveWorktreeDialog mounted ONCE at the App root, not in SessionSidebar) before calling archiveChat; throwing on 'cancel' rolls back aui's optimistic archive. D11 — Pin + right-click context menu in scope; shadcn ContextMenu wraps ThreadListItemPrimitive.Root with Pin/Unpin/Rename/Archive/Copy Session ID; the Tags action opens TagPopover via the use-tag-popover-target store + a single TagPopoverHost at root. D12 — filter pill is view-only (no auto-activate); no direct-create shortcuts on New-click; blank/last-thread fallback when the active chat is archived; cross-project activate clears the project filter (setFilterProjectId(null)); granular responsive row hiding shipped. Keep-warm: controllers stay in the registry across switches; the WS subscription is gated to the active thread only (ChatWsSubscription helper; controller never subscribes a __LOCALID_* chat). New uses new-thread-coordinator.createForLocal for the __LOCALID_* → remoteId handoff on first send (id stays __LOCALID_* for life — controller.setRemoteId stamps remoteId, no id-flip). **Grouping = TIME by default, with an opt-in Project mode** (2026-07 rebuild): Pinned/Today/Yesterday/Earlier via `arrangeSessions` stays the default; the Sort By menu now has a 4th option ("Project") that groups sessions into one section per project (project-list order, pinned still first) via the same `arrangeSessions` — additive, doesn't change the other 3 modes. Project narrowing is primarily the **project switcher list** (`ProjectFilterPillBar.tsx` — despite the name, a vertical one-click list now, not a pill-cloud: "All projects" row + one row per project with a colored initial avatar + attention badge, collapsible past 5 via "Show N more"). **Single-row session rows (2026-07 compaction):** the row is one line (StatusDot + title + time); worktree/PR/tag info collapsed from a second meta line into compact trailing glyphs (`SessionRowMetaIcons` — icon-only, no per-row project chip anymore); the full text (project, worktree/branch, PR, tag pills, branch-safety warning) now lives in a `SessionMetaCard` shown on hover. **Design-conformance pass done** (rendered-and-diffed vs `02-chrome.jsx` SessionRowDense/Sidebar): translucent panel (`bg-background/85` + `backdrop-blur-xl` since the 2026-08-07 token retirement), **collapsible wrapping tags bar (+N more — no horizontal scroll)**, thin warm auto-hide scrollbar (`mf-thin-scrollbar`). **Boot auto-opens the most-recent session** (parity with desktop) so the app never lands on an empty thread. **Window chrome DONE (2026-06-08):** native traffic lights (`tauri.conf.json` `trafficLightPosition {x:20,y:30}`, centered on the surface switcher) + **floating panels** (`AppShell` `p-2 gap-2`) + `SidebarHeader`/`ChatHeader`/`SurfaceTabStrip`/`SurfacePicker` + bottom `AppStatusBar` + design glyphs (`surface-icons.tsx`). **DEFERRED sidebar chrome (separate leaves, NOT built — full list lives in `docs/architecture/MIGRATION-TRACKER.md`, don't duplicate it here):** the remaining **Run surface CONTENT** (the Files editor and the Run **terminal** are DONE 2026-06-11/06-13; the **sandbox preview** tab — embedded Tauri webview — is the last Run-content leaf), the warm **radial-gradient window background** behind the floating panels (today flat `bg-background`), the ghosted/dashed "Add project" pill (needs the add-project flow — `features/sessions/sidebar/ProjectFilterPillBar.tsx`), and the SessionSidebar group-header "more" popover (`SessionSidebar.tsx` — a presentational placeholder button, no menu wired). These complete full-sidebar artboard parity but are larger surfaces beyond the sessions list. **The bottom Context/Skills/Agents tabbed panel is no longer deferred — it is ABSORBED** (2026-08-06, right-sidebar revamp): its content moved into the right-hand session panel (`features/session-panel/`), and `features/context-panel/` is deleted. Two capability decisions came with it. **D14:** in-session file mentions survive as a "Session" sub-group under Context — `derive-session-items.ts` and `ContextFileItem.tsx` moved into the panel rather than dying. **D15: agents are dropped from the product surface entirely** — no list renders `AgentConfig` after this PR, `AgentsList` is gone, and `useSidebarSkills` no longer fetches them. `lib/api/agents.ts` and its daemon route stay: the Setup Advisor still uses the endpoint. That is a deliberate, visible capability loss, not an oversight.
- **Markdown / reasoning / parts** → assistant-ui part components; restyle. **Reasoning: DECIDED — show the native `Reasoning` block, collapsed by default** (drop the dead `ThinkingPart`).

**Framing (don't repeat the over-conservative mistake):** the native *runtime-integration hooks* — `ModelContext.register`, `useThreadTokenUsage`, `useAssistantInstructions` — are **INERT under our external-store runtime** (they target the AI-SDK transport). But the native *presentational components* work fine. So the pattern is almost always **native component + our CLI/daemon data + our config-write path**, NOT "keep ours." e.g. `ModelSelector.Root` (composable, not the default export) fed our models; `ContextDisplay.Bar` with its external `usage` prop fed our CLI usage; `MessageTiming` fed `metadata.timing`; native `Image`/`File` parts fed `convert-message`; the `SyntaxHighlighter`/`CodeHeader` slots fed our shiki engine. Per-feature verdicts (6 adopt-native / 9 native-shell+our-data / 9 genuine keep-ours) live in `docs/architecture/ASSISTANT-UI-INVENTORY.md`.

*(Verified against `@assistant-ui/react@0.14.14` / `core@0.2.10`, 2026-06-05; re-verified on `react@0.14.27` / `core@0.2.21`, 2026-07-22.)*

---

## Runtime & data
- **`useExternalStoreRuntime`** fed by a **per-chat controller** (the `react-opencode` shape: controller + pure reducer + `handle-daemon-event` + projection). **Not** AssistantTransport.
- **No app-side message cache.** The daemon is the single source of truth; the controller holds only the per-chat projection, disposed on switch. Drift is handled by **refetch-on-gap** (unknown-id delta → REST re-seed), not a client cache or a daemon `seq`.
- **Optimistic-send reconcile = count-aware + server-authoritative** (judo-A). One matcher clears optimistic pendings against confirmed server user-messages via a multiset: each server copy clears at most one pending, oldest-first. The live `message.added` passes a single message; the history re-seed passes the full list. **No time window, no empty-text wildcard, no over-clearing** of legitimate duplicate sends.
- Sessions list via **`useRemoteThreadListRuntime`** + `chats-remote-adapter` (step 11, built). Global runtime mounted ONCE in App.tsx (App is the real root: DaemonPortProvider → AssistantRuntimeProvider). `useChatRuntimeHook` resolves per-thread context via `useAuiState((s) => s.threadListItem)` and keys the controller by the stable `item.id`; WS sub gated to the active thread (isActive = mainThreadId === item.id && item.remoteId != null). WS list side-effects (reload/unread/cross-project-filter-clear/archived-active-fallback) run via `useSessionListRouter()` under the provider.

## Architecture
- **Feature-first** under `features/`; `layout/` + `surfaces/` only *compose* features into the two typed surfaces (Chat/Workspace). No feature imports `layout/`.
- **Surface intent, not reach-through:** a feature that needs to open a file/diff/surface emits an intent; never `someStore.getState().openX()`.
- **`lib/tauri/` is the only Tauri-aware module** — all `window.mainframe.*` equivalents live there (Rust commands/events).
- **Pure logic** (convertMessage, diff math, file-types) goes in a shared bundleable location, **not** the `mainframe-core` *sidecar process*.

## Surface model (Chat · Workspace)

**TWO surfaces since the 2026-08-05 merge.** `SurfaceId` is `'chat' | 'workspace'`; the Files and Run surfaces are one `WorkspaceSurface` holding files, diffs, viewers, terminals, consoles, previews and URL tabs in a single pane model. Files/Run no longer exist as ids, components, stores, or drop targets.

- **One tab/pane model:** `store/run-pane.ts` (1–2 panes along an axis, per-tab `scopeKey`) plus `store/run-pane-file-tabs.ts` for the file-tab semantics — one preview slot per pane per launch scope, open-again focuses, permanent appends, `promoteFileTab` on double-click or first edit. The parallel Files tab store is deleted; there is no `useTabsStore`, and nothing bridges tabs between surfaces. `addRunTab` keeps its signature (consumer contract). Read a file tab out of the model with `activeFileTab(run)`, never by reaching into panes at a call site.
- **Every tab is scope-stamped** at creation from the active session (`useActiveBasesStore.scopeKey`), and the surface renders only tabs matching the active scope (`filterRunByScope`). "My open files" is per project/worktree, not per chat.
- **Hiding is not closing.** `toggleSurface('workspace')` preserves the panes and kills nothing: the surface holds the user's open files, and the terminal cache detaches without disposing. Kill-before-remove (`terminal-cleanup.ts`) and tunnel release (`url-tunnel-cleanup.ts`) belong to the real close paths — `closeRunTab`, `closePane`, `releaseRunScope`.
- **Dynamic floor — NOT a hardcoded chat floor (decision corrected 2026-06-21).** The non-dismissable "floor" is **whichever surface is the *last lit one*** (`isFloor = lit && litCount === 1`), not `id === 'chat'`. **Chat is hideable**: `ChatCardHeader` carries a **Hide Chat** button (`EyeOff`, `chat-header-hide`) and the workspace strip carries `workspace-surface-close`; both are disabled at the floor. If a toggle would empty the row, fall back to showing chat (`store/layout.ts` restore-`['chat']`).
- **Persistence:** `mf:session-layout` is at **v2**. Its migration folds persisted `files`/`run` ids into `workspace` (dedupe, drop a now-duplicate bottom strip, carry flex weights) and passes persisted tabs through — they already carry the `RunTab` shape. Only the safe kinds are written (`SAFE_RUN_TAB_KINDS`); a tab with no `mode` reads as permanent.
- **Placement is SETTLED (2026-08-08, todos #308/#309).** Multi-session is chrome-style **session tabs in the MainToolbar** (`features/session-tabs/` — the active session is the focused tab, ONE chat surface); side-by-side chat is REJECTED. `layout-placement.ts` deliberately KEEPS the three-slot algebra (top row of 1–2 + bottom strip + flex weights) so the workspace can sit beside or below the chat; both surfaces are draggable (the chat header grip emits `begin-surface-drag`, chat never goes to the bottom strip). The Files tree lives INSIDE the workspace surface as a right-edge local sidebar (`WorkspaceFilesSidebar`; `ui-prefs.workspaceFilesCollapsed`); the app-level InspectorPane is deleted. Surfaces never exchange tabs. Plan: `docs/plans/2026-08-08-session-tabs-and-workspace-files.md`; prototype: branch `proto/workspace-files-tabs`.

## Component layer & theme
- **shadcn/ui**, not raw Radix. Build the `components/ui/` primitives once; features compose them.
- Theme via the tokens in `src/styles/globals.css`. **Read the `mainframe-design-system` skill before writing markup or class names** — it carries the scales, the recipes, and the traps.
- **Token trap:** use only `mf-*` names that `@theme inline` actually maps; a typo renders as nothing, with no error. The same holds for stock names Tailwind never generated — `text-xl` is a live example. As of 2026-08-07 the bridge holds only domain palettes and app chrome; every generic `mf-*` colour and the whole v1 type scale are gone (use `text-xs/sm/base/lg`). The `/opacity` modifier *does* work here (Tailwind v4 compiles it to `color-mix`) — the old blanket prohibition was a v3 carryover from `packages/app-electron`. It only misleads on `accent`/`border`/`input`, which are already alpha.

## Conventions
- **No file > 300 lines**, no function > 50. Decompose god-files on port (don't carry them).
- **Every interactive element needs a stable `data-testid`** (`<surface>-<element>`); `ui/` primitives stay passthrough.
- **`convertMessage` invariants are load-bearing:** WS14c dual re-encode (top-level AND nested in task_group), the `\0` permission sentinel, per-message `uniqueId()` dedup, ≥1-content-part + error fallbacks.
- **Pin the whole `@assistant-ui/*` set together, exact** (currently `react@0.14.27` / `core@0.2.21`; was the `0.14.14`/`0.2.10` line). They're independently versioned and sub-1.0 — never let `^` ranges drift them apart, and **never land on `0.14.19`/`0.14.20`** (React-Compiler subscription bug, fixed in `.21`). `react-markdown` must move in lockstep (`0.14.6` needs `react ^0.14.18`).
- **Drop, don't port,** obsolete code: the `zone/` layout, layout/ui stores, the dual tool dispatcher, regex go-to-definition.

## Known gaps / phantom deps
The **full deferred backlog (every un-built surface + open decision) lives in `docs/architecture/MIGRATION-TRACKER.md`** — cross-reference it, don't duplicate it here. The few that bite *inside this package* day-to-day:
- **Deprecated assistant-ui hooks still in use** — sessions use `useAssistantRuntime().threads` because `useThreadListRuntime` isn't publicly exported; `useThreadListItemRuntime`/`useThreadRuntime`/`useMessageRuntime` are on deprecated paths. Migrating to the `useAui`/`useAuiState` selectors is a tracked backlog item, **not yet actioned** — keep the exact pin (0.14.27) until then.
- **Restored-permission "stream closed" gap** — replying to a *restored* permission whose CLI process died (daemon restart between question and answer) fails with "stream closed"; it **self-recovers on reload**, and plain reconnect with the CLI alive works. **PUSHED OUT to Post-V1 (2026-06-23)** — not a V1 blocker; see the tracker's *Pushed out — Post-V1* section.

## Daemon contract
The WS/REST contract is **co-owned by the mobile submodule** — changes must be **additive** (mobile ignores unknown fields). No unilateral reshaping.

## Definition of done (per ported surface)
Typecheck + tests green · matches the prototype artboard (design-conformance) · passes the thermo-nuclear standards · data-testids preserved · no `getState()` reach-through · files < 300 lines · obsolete code dropped · tracker updated.
