/**
 * Thin re-export — moved to `@qlan-ro/mainframe-types` in Phase 6 (see
 * `domain/tokens.ts`'s header comment for why).
 */
export { chipTextToText, normalizeDefinitionChipText, textToChipText } from '@qlan-ro/mainframe-types';
