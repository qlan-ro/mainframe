/**
 * WorkflowEditor — stub placeholder until Task 14.
 * Renders nothing; exists so WorkflowsModalHost can import it without error.
 */
export function WorkflowEditor(_props: { port: number; target: unknown }): null {
  return null;
}
